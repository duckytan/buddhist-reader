# v2.1 方案深度分析 — 任务清单

> 基于 v2.0 方案 + 2026-05-02 决策会议结果 + `2026-05-02-v2-dictionary-architecture-design.md`
> 目标：质量优先，用时间换质量
> 版本：v4（结构化升级版，增加任务元数据、安全/合规/开发体验分析、关键路径标注、统一产出模板）

---

## 元数据说明

| 字段 | 说明 |
|------|------|
| **优先级** | P0（阻塞后续开发，必须先完成） / P1（重要但不阻塞） / P2（锦上添花，可延后） |
| **预估工时** | 按调研+分析+写文档总耗时估算 |
| **关键路径** | 标注该任务是否阻塞其他任务或 Phase |
| **验收标准** | 任务完成的明确判断标准 |

---

## 第一阶段：技术功能调研（15 个任务）

### T-01 | Vue 3 架构 + Pinia 状态管理（合并原 T-01 + T-04）
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-13、T-46
**任务**：调研 Vue 3 + Composition API + Pinia 在阅读器类项目中的最佳实践
- 调研同类 Web 阅读器/电子书平台的 Vue 3 架构模式
- Composition API vs Options API 选择依据
- 组件拆分粒度：大组件 vs 微组件
- provide/inject 在深层组件通信中的使用边界
- Pinia 单 store vs 多 store 的划分原则
- state 数据量控制：哪些放 store 哪些放局部 state
- 跨页面数据同步：阅读进度、词典开关状态的持久化
- store 的懒加载：按需注册
- 对比：微信读书 Web 版、Kindle Cloud Reader 等项目的技术架构
**验收标准**：
- [ ] 明确项目目录结构和组件层级
- [ ] 确定 Pinia store 划分方案（5个store是否合理）
- [ ] 产出文档包含至少3个同类项目的架构对比

### T-02 | Vite 5 构建配置优化
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：深度调研 Vite 5 构建优化策略
- code splitting 策略：按路由 vs 按功能 vs 按大小
- 预加载/预取策略（modulepreload）
- mdict-js + lzo-wasm 的 ESM 兼容性处理
- 资源压缩：terser vs esbuild 对比
- 大型二进制依赖（MDX 解析）的懒加载策略
- 构建产物分析工具：rollup-plugin-visualizer
**验收标准**：
- [ ] 明确 mdict-js 的兼容配置（define: { global: 'globalThis' }）
- [ ] 确定 code splitting 策略和首屏 bundle 大小目标（< 200KB gzip）

### T-03 | Vant 4 UI 组件库深度评估
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-47
**任务**：评估 Vant 4 是否满足所有 UI 需求
- 禅意风格与 Vant 默认样式的冲突和定制方案
- 移动端优先 vs 响应式适配的最佳实践
- 组件按需引入 vs 全量引入的 bundle 影响
- 主题定制：warm tone 配色方案的实现路径
- 对比其他 UI 库：NutUI（京东）、Element Plus、Naive UI
**验收标准**：
- [ ] 确认 Vant 4 覆盖所有页面组件需求
- [ ] 确定禅意风格覆盖方案（CSS 变量 or ConfigProvider）

### T-04 | IndexedDB 存储方案（细化原 T-05）
**优先级**：P0 ｜ **预估工时**：4h ｜ **关键路径**：阻塞 T-27~T-34、T-45
**任务**：对比 idb、Dexie.js、localForage、原生 IndexedDB，确定最优方案
- 性能对比：大数据量（10万+词条）的读写速度
- 事务处理能力与回滚机制
- 版本迁移机制：13 张表的结构设计验证
- 内存占用分析：10万+词条索引 vs 全量数据的内存模型
- 浏览器兼容性：iOS Safari、Android WebView 的差异
- 错误恢复和降级策略：IndexedDB 不可用时 fallback 到 localStorage
- 边界情况：存储空间满、并发读写冲突、数据损坏恢复
- 是否需要引入离线优先框架（如 RxDB）
**验收标准**：
- [ ] 13张表结构验证通过，无索引冲突
- [ ] 10万条 dict_entries 批量写入耗时 < 2s
- [ ] 明确降级方案（localStorage fallback 的边界）

### T-05 | 高亮引擎完整方案（合并原 T-06 + T-10）
**优先级**：P0 ｜ **预估工时**：4h ｜ **关键路径**：阻塞 T-17、T-27、T-31
**任务**：研究 Trie 树 + 高亮渲染在经文阅读场景的最优实现
- 前端 Trie 实现对比：自定义实现 vs 开源库（trie-search、fast-levenshtein 等）
- 中文分词与 Trie 匹配的结合
- 长词优先策略的算法优化
- Trie 内存占用评估：10万词条 × 轻量索引（含 dictId/pinyin/category）的内存模型
- 对比 AC 自动机（Aho-Corasick）：多模式匹配的算法选择
- Web Worker 中运行 Trie 的可行性
- 虚拟滚动中的高亮性能优化
- 高亮颜色方案：多词典区分（4色映射）
- 点击高亮词条的事件处理与定位
- 高亮与文本选择（selection）的冲突处理
- 大段落经文（千字+）的高亮渲染性能
**验收标准**：
- [ ] 确定 Trie vs AC 自动机的最终选择
- [ ] 10万词条构建耗时 < 100ms
- [ ] 高亮响应时间 < 50ms

### T-06 | MDX 词典解析方案（细化原 T-07）
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-28、T-32、T-34
**任务**：研究 MDX 词典格式的解析方案，覆盖兼容性和性能
- mdict-js 的解析能力边界和已知问题
- lzo-wasm 的解压性能和兼容性
- MDX 格式规范研究：header 结构、词条索引、释义 HTML
- 对比其他 MDX 解析方案：python-mdict、readmdict
- 预解析性能测试：10万词条的解析时间和结果大小
- MDX 转 Markdown 的准确性和格式保留
- 兼容性陷阱：特殊编码、嵌套标签、内嵌资源的处理
- 大文件（>5MB / >10MB）解析的内存峰值控制
**验收标准**：
- [ ] 明确 mdict-js 支持的 MDX 编码范围
- [ ] 10万词条预解析耗时 < 10s（Web Worker 中）
- [ ] 确定内存峰值控制方案

### T-07 | Markdown 格式方案（合并原 T-08 + T-28）
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-09、T-18
**任务**：对比 Markdown 渲染引擎并验证统一 Markdown 方案的可行性
- 渲染引擎对比：markdown-it、marked、remark、micromark
- 渲染性能对比（移动端）
- HTML 直通支持质量
- 插件生态（梵文/藏文特殊标注支持）
- XSS 防护能力（markdown-it `html: true` 时的注入风险）
- SSR 兼容性（为未来预留）
- HTML → Markdown 转换的准确性测试（针对佛教词典的复杂 HTML）
- 梵文/藏文特殊格式保留测试
- 对比：直接存 HTML 的方案
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 确定 Markdown 渲染引擎
- [ ] XSS 防护方案明确（sanitize or CSP）
- [ ] 梵文/藏文特殊格式保留测试通过

### T-08 | Web Speech API TTS 引擎优化
**优先级**：P2 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：研究 Web Speech API 在中文/梵文诵读中的最佳实践
- 中文语音质量评估（各浏览器差异）
- 梵文/巴利文诵读支持
- 语速、音调、音量控制
- 断句和停顿优化（经文特殊需求）
- 对比商业 TTS API（Azure、Google Cloud、讯飞）
- 离线 TTS 方案（浏览器内置 vs 需要网络）
**验收标准**：
- [ ] 明确各浏览器 TTS 质量差异
- [ ] 确定断句策略（按标点 vs 按语义）

### T-09 | turndown 库调研（新增）
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-16（MDX 导入部分）
**任务**：调研 turndown 作为 HTML→Markdown 转换工具的可行性
- turndown 的核心功能和 API 设计
- 复杂 HTML 结构转换准确性：嵌套列表、表格、引用块
- 特殊格式保留：`<span class="sanskrit">` 等梵文标签的透传处理
- 性能测试：1万+条 HTML 释义批量转换的耗时
- 对比其他转换工具：html-to-md、heaven、showdown reverse
- 自定义规则扩展：如何处理佛教词典特有的 HTML 结构
- 边界情况：空标签、内联样式、base64 图片的处理
**验收标准**：
- [ ] turndown 自定义规则方案确定
- [ ] 1万条 HTML 转换耗时 < 5s

### T-10 | LRU 缓存策略调研（新增）
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-45
**任务**：研究前端 LRU 缓存策略在释义加载场景的最佳实践
- 前端 LRU 缓存实现方案：Map + 自定义 vs 开源库（lru-cache 的浏览器适配）
- 缓存容量上限设计：目标上限 1000 条，内存占用评估
- 缓存淘汰策略：LRU vs LFU vs TTL 的选择
- 缓存键设计：`${dictId}::${term}` 的冲突处理
- 缓存预热策略：高频词条预加载的可行性
- 缓存与 IndexedDB 的一致性：DB 更新时缓存失效
**验收标准**：
- [ ] 确定 LRU 实现方案
- [ ] 1000 条缓存内存占用 < 10MB
- [ ] 缓存失效策略明确

### T-11 | Web Worker 技术方案（新增）
**优先级**：P1 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-41（错误处理部分）
**任务**：调研 Web Worker 在词典解析和 Trie 计算中的应用
- Web Worker 的创建和通信模式：postMessage vs MessageChannel vs Transferable
- 大文件（10MB+ MDX）解析的 Worker 方案
- Trie 构建（10万词条）放入 Worker 的可行性
- Worker 中的模块加载：ESM import vs importScripts
- 进度反馈机制：Worker → 主线程的进度更新
- 移动端兼容性：iOS Safari、Android WebView 对 Worker 的支持度
- SharedArrayBuffer 的可行性（需 COOP/COEP 头）
**验收标准**：
- [ ] 确定 Worker 通信模式
- [ ] 移动端 Worker 兼容性验证通过
- [ ] 进度反馈方案确定

### T-12 | v1.0 代码与数据逆向分析（新增）
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-48（方案整合）
**任务**：分析 v1.0 存档代码，提取可迁移的数据和逻辑
- 从 `archive/v1.0/src/data/dictionary.js` 提取 50+ 内置词典数据
- 从 `archive/v1.0/src/data/sutras.js` 提取 5 部经书元数据和章节内容
- 从 `archive/v1.0/src/data/pronunciation-map.js` 提取读音映射
- 从 `archive/v1.0/src/utils/trie.js` 分析 Trie 实现逻辑
- 从 `archive/v1.0/src/utils/storage.js` 分析 localStorage 数据结构
- 从 `archive/v1.0/src/utils/tts.js` 分析 TTS 封装逻辑
- 提取禅意 UI 配色方案、CSS 变量、字体栈
- 对比 v1.0 与 v2.0 的差异，列出需要重构的代码块
**验收标准**：
- [ ] 内置词典数据完整提取
- [ ] 5部经书元数据和章节内容完整提取
- [ ] 迁移清单明确（哪些直接复用，哪些需要重构）

### T-13 | EventBus 跨组件通信方案（新增）
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-16（开关部分）
**任务**：研究词典开关→高亮实时刷新的事件通信机制
- Vue 3 推荐的事件模式：mitt、emittery vs provide/inject vs Pinia
- 词典开关切换触发高亮刷新的事件时序
- 阅读页在后台运行时的事件订阅和清理
- 事件防抖和节流：频繁开关切换时的处理
- 对比：Vuex 事件总线模式 vs Pinia 事件模式
**验收标准**：
- [ ] 确定事件通信方案
- [ ] 事件时序图明确
- [ ] 防抖策略确定

### T-14 | 前端安全分析（新增）
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-7、T-41
**任务**：系统性分析项目前端安全风险和防护策略
- XSS 防护：markdown-it `html: true` 的注入风险，DOMPurify 集成方案
- MDX 文件安全：用户上传的 MDX 可能包含恶意脚本（script 标签、onerror 事件、javascript: 协议）
- CSP 策略：Vercel 部署时的 Content-Security-Policy 配置
- localStorage/IndexedDB 数据加密：用户笔记是否需加密存储
- 文件上传安全：文件类型验证、大小限制、内容检测
- 第三方依赖安全审计：mdict-js、lzo-wasm、markdown-it 等 npm 包的安全性
- 对比：同类 Web App 的安全策略
**验收标准**：
- [ ] XSS 防护方案明确（sanitize + CSP）
- [ ] MDX 文件安全检测流程确定
- [ ] CSP 策略草案

### T-15 | 版权与合规分析（新增）
**优先级**：P2 ｜ **预估工时**：1h ｜ **关键路径**：无
**任务**：分析项目涉及的版权和合规问题
- MDX 词典来源合法性：用户上传的词典是否涉及版权问题，如何处理
- 内置词典授权：50+ 佛教术语的原始来源和授权状态
- 经文文本版权：5部佛经（心经、金刚经等）的版权状态（公开领域 vs 需授权）
- 用户隐私：IndexedDB 存储的用户数据（阅读进度、笔记）是否需要隐私政策
- 开源协议：项目 MIT 协议的兼容性检查
**验收标准**：
- [ ] 内置词典和经文版权状态明确
- [ ] MDX 用户上传的免责声明/合规指引
- [ ] 隐私政策需求确认

---

## 第二阶段：功能需求分析（13 个任务）

### T-16 | 书架页面设计
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-48
**任务**：调研电子书书架的业界最佳实践
- 网格布局 vs 列表布局
- 封面设计：自动生成 vs 用户上传
- 排序和筛选：最近阅读、字母、分类
- 书架分组功能
- 对比：微信读书、Kindle、Apple Books 的书架设计
**验收标准**：
- [ ] 确定书架布局方案（响应式）
- [ ] 封面生成策略确定

### T-17 | 阅读页面设计
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-25、T-47
**任务**：研究经文阅读页面的最佳交互设计
- 字体选择和字号调节
- 行间距、段落间距、页边距
- 夜间模式 / 护眼模式
- 翻页 vs 滚动
- 目录导航
- 阅读进度显示
- 对比：微信读书、得到、掌阅的阅读页设计
**验收标准**：
- [ ] 阅读页交互原型确定
- [ ] 字体、字号、行间距范围确定
- [ ] 翻页/滚动方案确定

### T-18 | 词典管理系统（合并原 T-13 + T-14 + T-15）
**优先级**：P0 ｜ **预估工时**：4h ｜ **关键路径**：阻塞 T-24
**任务**：设计词典管理页面的完整交互，涵盖管理、导入、开关全流程
- 词典列表展示方式（名称、类型、词条数、开关状态）
- 开关交互细节（Vant Switch 滑动动画、确认提示）
- 关闭时受影响词条数的计算和显示："关闭后将少高亮 X 个词条"（D12）
- 全部启用 / 全部禁用按钮
- 开关切换 → Trie 更新 → 高亮刷新的时序（D7 立即生效）
- 切换时的 UI 过渡动画
- 文件导入支持格式：JSON / CSV / MDX 的处理流程
- 文件类型检测和验证
- 大文件上传进度显示
- 导入前的预处理和校验
- 导入失败的回滚机制
- 对比其他词典软件的导入流程（GoldenDict、欧路词典）
- 版本历史展示（D14）
- 删除确认和二次验证
**验收标准**：
- [ ] 词典管理页面交互流程完整
- [ ] 开关→高亮刷新时序明确
- [ ] 导入流程（含进度、回滚）明确

### T-19 | 词条高亮匹配
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计高亮匹配的完整流程
- 匹配规则：精确匹配 vs 模糊匹配
- 嵌套词条处理（"般若"和"般若波罗蜜多"重叠）
- 标点符号前后的匹配边界
- 高亮样式：颜色、下划线、背景色
- 多词典同词条的高亮区分
**验收标准**：
- [ ] 长词优先去重策略确定
- [ ] 高亮颜色映射表（4色）确定

### T-20 | 释义展示层（合并原 T-17 + T-18）
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-47
**任务**：设计释义弹窗与用户笔记层的完整交互
- 弹窗位置和定位策略（跟随高亮词条）
- 流式加载的 UI 反馈（先返回的先渲染）
- 3 种展示模式的交互细节（全部折叠 / 展开第一个 / 全部展开）
- 弹窗内 Markdown 渲染质量
- 移动端弹窗的适配（底部抽屉样式）
- 对比：欧路词典、有道词典的弹窗设计
- 用户笔记与原始释义的分离和合并展示
- Markdown 编辑器选型（轻量级）
- 笔记的搜索和导出
- 笔记的版本历史
- 对比：Notion、Obsidian 的笔记理念
**验收标准**：
- [ ] 3种展示模式的交互细节确定
- [ ] 移动端弹窗样式确定
- [ ] 笔记编辑器选型确定

### T-21 | 书签功能
**优先级**：P2 ｜ **预估工时**：1h ｜ **关键路径**：无
**任务**：设计书签的完整功能
- 书签添加方式（手动 / 自动）
- 书签分类和标签
- 书签列表的展示和检索
- 书签导出和导入
- 对比：主流阅读器的书签设计
**验收标准**：
- [ ] 书签添加和管理流程确定

### T-22 | 经文搜索功能
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：研究经文内容搜索的实现
- 全文搜索 vs 关键词搜索
- 搜索结果的展示方式
- 高亮搜索结果
- 搜索历史
- 对比：电子书搜索方案
**验收标准**：
- [ ] 搜索方案确定（全文搜索 vs 关键词）
- [ ] 搜索结果展示方式确定

### T-23 | 功德统计页面
**优先级**：P2 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计功德统计的展示和算法
- 统计维度：按天/周/月/年
- 可视化图表选型（Chart.js / ECharts / 其他）
- 连续诵读天数计算
- 功德排行榜（可选）
- 数据导出
**验收标准**：
- [ ] 统计维度确定
- [ ] 图表库选型确定

### T-24 | 设置页面
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计设置页面的功能组织
- 功能分类：阅读设置 / 词典设置 / 显示设置 / 数据管理
- 设置项的持久化和同步
- 设置导入导出
- 数据清理和重置
**验收标准**：
- [ ] 设置页面功能分类确定
- [ ] 持久化策略确定

### T-25 | 经书导入功能（新增）
**优先级**：P2 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计经书上传和导入的完整流程
- 支持格式：txt（按章节标记分割）
- 章节分割策略：正则匹配章节标题 vs 手动配置
- 元数据提取：经名、译者、卷数
- 重复经书检测：同经名、不同译者的处理
- 大文件（万字+经文）的解析性能
- 导入失败回滚
- 对比：其他阅读器（微信读书、掌阅）的导入体验
**验收标准**：
- [ ] 经书导入流程确定
- [ ] 章节分割策略确定

### T-26 | 词典版本管理（新增）
**优先级**：P2 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计词典版本链的展示和回滚机制
- `dict_versions` 表的交互设计：版本列表、上传时间、词条数对比
- 版本回滚功能：恢复到历史版本的数据操作
- 版本差异对比：新旧版本词条变化可视化
- 保留历史版本 vs 清理旧版本的空间管理策略
- 用户笔记在版本回滚时的处理（是否保留）
**验收标准**：
- [ ] 版本回滚流程确定
- [ ] 用户笔记在回滚时的处理策略确定

### T-27 | 目录导航侧栏（新增）
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计阅读页的目录导航交互
- 经文章节列表的展示（单章节 vs 多章节）
- 快速跳转：点击章节直接定位到对应内容
- 当前章节高亮标记
- 侧栏的展开/收起动画（移动端适配）
- 长经书（如金刚经 32 品）的目录折叠策略
- 对比：微信读书、Kindle 的目录交互
**验收标准**：
- [ ] 目录导航交互确定
- [ ] 长经书折叠策略确定

### T-28 | D13 版本智能合并分析（新增）
**优先级**：P1 ｜ **预估工时**：3h ｜ **关键路径**：无（远期）
**任务**：研究内置词典版本更新时与用户笔记层的冲突处理策略
- 场景 1：内置词条 definition 变更，但用户写过笔记 — 如何合并
- 场景 2：内置词条被删除，但用户有笔记 — 笔记是否保留
- 场景 3：内置新增词条，用户没有笔记 — 自动添加
- 场景 4：用户修改过的词条，在新版本中内容部分重叠 — 差异对比
- 使用 `original_checksum` 字段判断词条是否变更的可行性
- 通知机制：提示用户"词典已更新，您的笔记可能与新内容不匹配"
- 对比：Obsidian、Notion 的版本冲突处理
**验收标准**：
- [ ] 4种冲突场景的合并策略确定
- [ ] 通知机制确定

---

## 第三阶段：已确认决策验证（8 个任务）

### T-29 | 验证：Trie 轻量索引决策
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-5、T-31
**任务**：验证"含 dictId + pinyin + category"的轻量索引方案
- 内存占用精算：10万词条 × 3字段的大小
- 对比最小索引方案的实际差异
- 调研其他词典 App 的索引策略
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 内存占用数据明确
- [ ] 最终结论确定（维持 or 调整）

### T-30 | 验证：MDX 全部预解析 + 切割决策
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-18
**任务**：验证"统一预解析 + 2MB 切割"方案
- 预解析 10万词条的性能测试
- 切割后查询路径的性能影响
- term → chunk 路由表的构建和维护成本
- 对比 GoldenDict、欧路词典的 MDX 处理方案
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 预解析性能数据
- [ ] 查询路径耗时数据
- [ ] 最终结论

### T-31 | 验证：流式加载 + 3 种展示模式
**优先级**：P1 ｜ **预估工时**：1h ｜ **关键路径**：无
**任务**：验证流式加载和展示模式设计
- 调研其他词典应用的释义展示方式
- 3 种模式的实际用户场景分析
- 设置项的存储位置（IndexedDB vs localStorage）
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 3种模式的适用场景明确
- [ ] 存储位置确定

### T-32 | 验证：Trie 每次启动重建
**优先级**：P0 ｜ **预估工时**：1h ｜ **关键路径**：阻塞 T-5
**任务**：验证"每次启动重建 Trie"方案
- 10万词条重建的实际耗时测试
- 对比持久化方案的版本同步复杂度
- 调研其他前端词典的重建策略
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 重建耗时数据
- [ ] 最终结论

### T-33 | 验证：单一 Trie + 运行时过滤
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-5、T-31
**任务**：验证单一 Trie + 运行时过滤方案的可扩展性
- 10万词条的 Trie 搜索性能
- 运行时过滤的开销
- 切换到分层 Trie 的接口设计预演
- 调研 AC 自动机是否更适合多词典场景
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 搜索性能和过滤开销数据
- [ ] 接口抽象方案
- [ ] 最终结论

### T-34 | 验证：D18 切割方案可行性（新增）
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-18
**任务**：验证单文件 10MB 限制 + 2MB 自动切割方案
- 10MB MDX 文件解析后的实际数据量膨胀系数
- 2MB 切割边界的精确控制：按条累加 vs 按大小切分
- 超大单条释义（>2MB）的单独存储方案
- `dict_term_lookup` 路由表的大小评估
- 切割后两步查询（lookup → entry）的总耗时是否 < 2ms
- 对比：是否需要引入 5MB 分界点（小文件预解析，大文件保留原文件）
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 数据膨胀系数数据
- [ ] 路由表大小数据
- [ ] 查询耗时数据
- [ ] 最终结论

### T-35 | 验证：移动端存储限制（新增）
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-36
**任务**：验证移动端 IndexedDB 配额限制对方案的影响
- iOS Safari IndexedDB 配额上限（约 1GB）和清理策略
- Android WebView 的存储限制差异
- 多词典累计存储量的估算：10万词条 × Markdown 释义
- 上传前检查可用空间的实现方案
- 存储空间不足时的自动清理策略（LRU 清理旧词典 vs 提示用户手动管理）
- 对比：其他移动端 Web App 的存储管理
- 结论：维持原方案 or 调整
**验收标准**：
- [ ] 各平台配额数据
- [ ] 存储量估算数据
- [ ] 清理策略确定

### T-36 | 验证：MDX 解析兼容性（新增）
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：验证 mdict-js 对主流 MDX 格式的兼容边界
- 收集 5-10 个真实佛教/国学 MDX 词典样本
- 测试不同编码：UTF-8、GBK、Big5 的解析成功率
- 测试特殊结构：嵌套 HTML、内嵌 CSS、图片引用、音频引用
- 测试 lzo-wasm 解压的稳定性：损坏文件的容错
- 不兼容格式的处理：提示用户转换 vs 自动跳过
- 对比：GoldenDict 对相同 MDX 文件的兼容性
- 结论：维持原方案 or 增加兼容层
**验收标准**：
- [ ] 各编码解析成功率数据
- [ ] 不兼容格式处理策略
- [ ] 最终结论

---

## 第四阶段：深度分析（13 个任务）

### T-37 | 性能优化全面分析
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-48
**任务**：系统性分析性能瓶颈和优化策略
- 首屏加载性能（FCP / LCP / TTI）：目标 < 1s
- 运行时性能：帧率、内存、长任务
- 高亮响应时间：目标 < 50ms
- 释义加载时间：目标 < 200ms
- 图片/字体资源优化
- Web Worker 的使用场景（Trie 匹配、MDX 解析）
- 浏览器缓存策略
- CDN 加速
- Lighthouse 评分目标
- 具体测试方法：Lighthouse CI、WebPageTest、Chrome DevTools Performance 面板
**验收标准**：
- [ ] 各性能指标目标值确定
- [ ] 测试方法和工具确定

### T-38 | 移动端适配全面分析
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-40
**任务**：深入研究移动端用户体验
- 响应式断点设计（手机/平板/PC）
- 触摸手势：滑动翻页、捏合缩放
- 移动端键盘适配（搜索、笔记编辑）
- 底部导航栏设计
- 移动端存储限制（IndexedDB 配额）
- iOS Safari 的特殊问题（PWA、存储、TTS）
**验收标准**：
- [ ] 响应式断点确定
- [ ] 手势和键盘适配方案

### T-39 | PWA 离线能力分析
**优先级**：P2 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：评估 PWA 离线支持的可行性
- Service Worker 策略：缓存优先 vs 网络优先
- 离线词典：已导入的词典完全离线可用
- 离线经书：已加载的经书完全离线可用
- PWA 安装提示
- 离线时的功能降级
- 对比：其他离线阅读 App 的实现
**验收标准**：
- [ ] Service Worker 策略确定
- [ ] 离线功能范围确定

### T-40 | 数据安全与备份
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计用户数据的备份和恢复机制
- 数据导出格式设计（JSON / CSV）
- 数据导入恢复流程
- 云同步预留接口
- 数据加密存储（敏感笔记）
- IndexedDB 数据损坏的恢复
**验收标准**：
- [ ] 导出/导入流程确定
- [ ] 加密策略确定

### T-41 | 可访问性（a11y）分析
**优先级**：P2 ｜ **预估工时**：1h ｜ **关键路径**：无
**任务**：评估和提升可访问性
- WCAG 2.1 AA 标准对照
- 键盘导航支持
- 屏幕阅读器兼容
- 色盲友好的高亮配色
- 字体大小调节范围
**验收标准**：
- [ ] a11y 标准对照结果
- [ ] 色盲友好配色方案

### T-42 | 禅意 UI/UX 设计语言
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-47
**任务**：定义项目的视觉设计语言
- 禅意设计原则提炼（极简、留白、温暖）
- 色彩系统：主色、辅助色、语义色
- 字体系统：中文字体、梵文字体
- 动画和过渡效果（克制、优雅）
- 图标风格
- 对比：同类佛教 App 的设计风格
**验收标准**：
- [ ] 设计 token 系统确定
- [ ] 色彩、字体、动画规范

### T-43 | 错误处理与异常恢复
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：设计全局错误处理策略
- 全局错误边界（Vue errorCaptured）
- 数据加载失败的重试机制
- 用户友好的错误提示
- 日志记录和上报（可选）
- 降级策略：IndexedDB 不可用时的 fallback
**验收标准**：
- [ ] 错误分类和重试策略
- [ ] 降级方案确定

### T-44 | 测试策略
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-48
**任务**：设计项目的测试方案
- 单元测试框架选型（Vitest）
- 组件测试（Vue Test Utils）
- E2E 测试（Playwright / Cypress）
- 测试覆盖率目标
- 核心模块的测试重点
**验收标准**：
- [ ] 测试框架确定
- [ ] 覆盖率目标确定

### T-45 | 部署与 CI/CD
**优先级**：P1 ｜ **预估工时**：1h ｜ **关键路径**：无
**任务**：优化部署流程和 CI/CD
- Vercel 部署配置优化
- 预览部署流程
- 环境变量管理
- 构建产物大小监控
- 自动化检查（lint、typecheck）
**验收标准**：
- [ ] 部署流程确定
- [ ] CI/CD 检查项确定

### T-46 | 未来扩展性分析
**优先级**：P2 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：为未来功能预留扩展点
- 微信小程序迁移的技术路径
- 后端 API 接入的接口设计
- 多语言国际化（i18n）预留
- 用户账户系统预留
- 词典分享功能预留（D22 不做清单的边界）
**验收标准**：
- [ ] 扩展点清单
- [ ] 预留接口设计

### T-47 | 缓存策略全面分析（新增）
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：系统性设计前端缓存策略
- LRU 缓存的实现和容量管理（上限 1000 条）
- 缓存命中率评估：高频词条 vs 低频词条
- 缓存清理时机：页面切换、内存压力、用户主动清理
- IndexedDB 作为二级缓存的可行性
- 缓存预热策略：用户进入阅读页时预加载高频词条
- 多词典场景下的缓存共享和隔离
**验收标准**：
- [ ] 缓存策略完整方案
- [ ] 清理和预热策略确定

### T-48 | Service 层接口抽象设计（新增）
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-48（方案整合）
**任务**：设计 Service 层的抽象接口，预留后端 API 切换能力
- 统一接口定义：DictService、SutraService、ProgressService、StatsService、TTSService
- 接口与实现分离：Service 接口 vs IndexedDB 实现 vs 未来 REST API 实现
- 数据格式标准化：所有 Service 返回统一的数据结构
- 错误处理统一化：Service 层的异常分类和错误码
- 接口版本管理：预留 v1、v2 的接口演进
- 对比：Clean Architecture 的 Repository 模式
**验收标准**：
- [ ] Service 接口定义完成
- [ ] 错误码体系确定

### T-49 | 禅意设计原则落地方案（新增）
**优先级**：P0 ｜ **预估工时**：2h ｜ **关键路径**：阻塞 T-48（方案整合）
**任务**：将禅意设计原则转化为具体的 UI/UX 实现方案
- v1.0 禅意 UI 的提取和保留：配色、字体、间距、圆角
- 设计 token 系统：CSS 变量定义禅意设计语言
- 暗色模式的禅意风格适配（非纯黑，保持温暖感）
- 护眼模式（暖色调）的色温和亮度参数
- 动画和过渡效果的"克制"标准：时长、曲线、触发条件
- Vant 组件的禅意风格覆盖方案
- 对比：其他禅意/极简风格 App（冥想 App、禅宗工具）
**验收标准**：
- [ ] CSS 变量系统确定
- [ ] 暗色/护眼模式参数确定
- [ ] Vant 覆盖方案确定

---

## 第五阶段：综合输出（2 个任务）

### T-50 | GitHub + Vercel 部署环境与限制分析（新增）
**优先级**：P0 ｜ **预估工时**：3h ｜ **关键路径**：阻塞 T-45（部署与 CI/CD）
**任务**：深度调研 GitHub + Vercel 部署的限制、规范和潜在问题
- Vercel Hobby 免费版限制：构建时间（45 分钟）、带宽（100GB/月）、构建产物大小、部署频率
- GitHub 仓库限制：单文件 100MB 限制、仓库建议 < 1GB、Git LFS 免费额度（1GB 存储 + 1GB/月带宽）
- MDX 词典文件处理策略：用户上传的 MDX 文件 > 100MB 时不能提交到 Git，需通过其他途径分发
- Vercel SPA 路由配置：`vercel.json` 的 `rewrites` 配置，防止刷新 404
- 预览部署机制：每个分支自动生成预览 URL，但 URL 变化导致 IndexedDB 隔离问题
- 环境变量管理：Vercel 环境变量 vs `.env` 文件，构建时 vs 运行时变量差异
- Node.js 版本兼容性：Vercel 默认 Node 版本与项目依赖的兼容性
- 构建超时处理：mdict-js + lzo-wasm 的 ESM/CJS 混合构建可能导致的构建失败
- CDN 缓存策略：Vercel Edge Network 缓存配置，静态资源 cache-control 头
- 自定义域名与 SSL：Vercel 自动 SSL 证书，CNAME 配置
- Vercel 监控与日志：Function Logs（不适用 SPA）、Analytics 集成
- GitHub Actions 限制：免费 2000 分钟/月，CI/CD 流水线优化
- **验收标准**：
  - [ ] Vercel Hobby 限制清单完整
  - [ ] MDX 大文件处理方案确定（不提交到 Git，用户浏览器本地导入）
  - [ ] SPA 路由配置验证通过
  - [ ] IndexedDB 跨域问题解决方案
  - [ ] 环境变量管理方案确定
- **产出**：`docs/plans/deep-dive/github-vercel-deployment-analysis.md`

### T-51 | v2.1 方案整合设计
**优先级**：P0 ｜ **预估工时**：6h ｜ **关键路径**：最终交付
**任务**：综合以上所有调研和分析，输出全新的 v2.1 方案
- 整合所有调研结果，对比 v2.0 的差异
- 明确架构变更
- 更新数据模型
- 更新开发阶段规划
- 更新性能目标
- 更新风险清单
**验收标准**：
- [ ] 方案整合文档完成
- [ ] 覆盖所有调研结论
- [ ] 风险清单更新

### T-52 | v2.0 → v2.1 变更说明
**优先级**：P1 ｜ **预估工时**：2h ｜ **关键路径**：无
**任务**：撰写从 v2.0 到 v2.1 的变更说明文档
- 逐条列出变更点
- 每条变更的原因和依据
- 对开发计划的影响
**验收标准**：
- [ ] 变更说明文档完成
- [ ] 逐条对应开发计划影响

---

## 任务执行顺序建议

```
第一轮（技术调研，可并行）:
  T-01 ~ T-15  （15 个任务，可独立并行执行）
  关键路径任务：T-01、T-04、T-05、T-06、T-07、T-12、T-14

第二轮（功能分析，可并行）:
  T-16 ~ T-28  （13 个任务，可独立并行执行）
  关键路径任务：T-17、T-18、T-20

第三轮（决策验证，依赖第一轮结果）:
  T-29 ~ T-36  （8 个任务，需要技术调研结果作为支撑）
  关键路径任务：T-29、T-30、T-32、T-33

第四轮（深度分析，可并行）:
  T-37 ~ T-50  （14 个任务，可独立并行执行）
  关键路径任务：T-37、T-38、T-42、T-48、T-49、T-50

第五轮（综合输出，依赖前面所有轮次）:
  T-51 ~ T-52  （2 个任务，需要前面所有调研和分析完成）
  关键路径任务：T-51
```

---

## 关键路径标注

以下任务按依赖关系排序，标注了哪些任务阻塞后续开发：

| 阶段 | 关键路径任务 | 阻塞后续 |
|------|-------------|----------|
| 技术调研 | T-01 架构+Pinia | T-13（EventBus）、T-48（Service 层抽象） |
| 技术调研 | T-04 IndexedDB | T-29~T-36（决策验证）、T-47（缓存策略） |
| 技术调研 | T-05 高亮引擎 | T-19（高亮匹配）、T-29（索引验证）、T-33（单一 Trie） |
| 技术调研 | T-06 MDX 解析 | T-30（预解析验证）、T-34（切割验证）、T-36（兼容性验证） |
| 技术调研 | T-07 Markdown 格式 | T-09（turndown）、T-20（释义展示层） |
| 技术调研 | T-12 v1.0 逆向分析 | T-50（方案整合） |
| 技术调研 | T-14 前端安全 | T-07（Markdown XSS）、T-43（错误处理） |
| 功能分析 | T-17 阅读页设计 | T-27（目录导航）、T-49（禅意落地） |
| 功能分析 | T-18 词典管理 | T-26（版本管理）、T-30（预解析验证）、T-34（切割验证） |
| 功能分析 | T-20 释义展示层 | T-49（禅意落地） |
| 决策验证 | T-29 Trie 索引验证 | T-05（高亮引擎）、T-33（单一 Trie） |
| 决策验证 | T-32 Trie 重建验证 | T-05（高亮引擎） |
| 决策验证 | T-33 单一 Trie 验证 | T-05（高亮引擎）、T-19（高亮匹配） |
| 决策验证 | T-35 移动端存储验证 | T-38（移动端适配） |
| 深度分析 | T-37 性能优化 | T-50（方案整合） |
| 深度分析 | T-38 移动端适配 | T-40（禅意 UI） |
| 深度分析 | T-42 禅意 UI 设计 | T-47（缓存策略）、T-49（禅意落地） |
| 深度分析 | T-44 测试策略 | T-50（方案整合） |
| 深度分析 | T-48 Service 层抽象 | T-51（方案整合） |
| 深度分析 | T-49 禅意落地 | T-51（方案整合） |
| 深度分析 | T-50 部署环境分析 | T-45（部署与 CI/CD） |

---

## 统一产出文档模板

所有调研任务产出文档应遵循以下结构（`docs/plans/research/`、`docs/plans/analysis/`、`docs/plans/verification/`、`docs/plans/deep-dive/` 目录下所有文档统一）：

```markdown
# [任务名称] 调研报告

> 任务编号：T-XX
> 完成日期：YYYY-MM-DD
> 基于：[相关决策文档/技术方案链接]

## 1. 背景与目标
[简要说明调研背景和目标]

## 2. 调研内容
### 2.1 [调研维度 1]
[内容]

### 2.2 [调研维度 2]
[内容]

### 2.3 [对比分析]
| 方案/工具 | 优点 | 缺点 | 适用场景 |
|----------|------|------|----------|
| ... | ... | ... | ... |

## 3. 测试数据
[如有性能测试、兼容性测试等，附上数据表格]

## 4. 结论与建议
[明确结论：维持原方案 / 调整方案 / 需进一步讨论]

## 5. 对 v2.1 方案的影响
[列出本调研结果对 v2.1 方案的具体影响]

## 6. 参考资料
[列出参考的文档、链接、测试样本]
```

---

## 总计

- **技术功能调研**：15 个任务（T-01 ~ T-15）
- **功能需求分析**：13 个任务（T-16 ~ T-28）
- **决策验证**：8 个任务（T-29 ~ T-36）
- **深度分析**：14 个任务（T-37 ~ T-50）
- **综合输出**：2 个任务（T-51 ~ T-52）

**合计：52 个可执行任务**（v1: 40 → v2: 34 → v3: 49 → v4: 51 → v5: 52）

---

## 版本变更说明

| 版本 | 任务数 | 变更 |
|------|--------|------|
| v1 | 40 | 初始版本 |
| v2 | 34 | 合并 6 个关联任务，细化 3 个任务 |
| v3 | 49 | 补全 15 个遗漏项 |
| v4 | 51 | 结构化升级：增加任务元数据、2个新任务（安全+合规）、关键路径标注、验收标准、统一产出模板 |
| v5 | 52 | 新增 T-50：GitHub + Vercel 部署环境与限制分析 |

### v5 新增内容

| 类型 | 内容 | 说明 |
|------|------|------|
| 新任务 T-50 | GitHub + Vercel 部署环境与限制分析 | Vercel Hobby 限制、GitHub 仓库限制、MDX 大文件处理、SPA 路由配置、IndexedDB 跨域问题、CDN 缓存策略、CI/CD 流水线等 |

### T-50 关键发现预判

| 问题 | 影响 | 解决方案 |
|------|------|----------|
| Vercel 构建产物大小限制 | 内置词典数据过大导致构建失败 | 词典数据不打包进构建产物，改为运行时动态加载 |
| GitHub 单文件 100MB 限制 | MDX 词典文件无法提交到仓库 | 用户通过浏览器本地导入，不经过 Git |
| IndexedDB 跨域隔离 | Vercel 预览 URL 变化导致数据不可访问 | 使用 `*.vercel.app` 统一域名，或实现数据迁移 |
| SPA 路由刷新 404 | Vue Router history 模式刷新丢失路由 | `vercel.json` 配置 `rewrites: [{ source: '/(.*)', destination: '/index.html' }]` |
| mdict-js ESM/CJS 混合 | Vercel 构建时模块解析失败 | Vite 配置 `optimizeDeps.include` + `commonjsOptions.transformMixedEsModules` |

### v4 新增内容

| 类型 | 内容 | 说明 |
|------|------|------|
| 任务元数据 | 每个任务增加优先级、预估工时、关键路径、验收标准 | 让任务可执行、可验证 |
| 新任务 T-14 | 前端安全分析 | XSS、MDX 文件安全、CSP、依赖安全审计 |
| 新任务 T-15 | 版权与合规分析 | MDX 版权、经文版权、用户隐私 |
| 关键路径 | 完整依赖关系表和阻塞链 | 明确哪些任务必须先完成 |
| 验收标准 | 每个任务 2-4 条明确标准 | 判断任务完成的依据 |
| 产出模板 | 统一调研报告文档结构 | 保证 51 份文档格式一致 |
