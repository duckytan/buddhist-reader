# 般若佛经阅读器 v2.0 — 测试方案

> **版本**：v1.0  
> **日期**：2026-05-03  
> **状态**：待评审  
> **编写依据**：`docs/v2.0-detailed-design.md`、`docs/specs/01-需求规格说明书.md`

---

## 1. 测试策略

### 1.1 测试分层

```mermaid
graph TB
    A["E2E 测试 (端到端)"] --> B["集成测试 (模块间)"]
    B --> C["单元测试 (函数/组件)"]
    
    style A fill:#e8f5e9
    style B fill:#fff3e0
    style C fill:#e3f2fd
```

| 测试层级 | 目标 | 工具推荐 | 覆盖率目标 |
|----------|------|----------|------------|
| 单元测试 | 验证函数、类、组件的独立行为 | Vitest + Vue Test Utils | > 80% 引擎层/Service 层 |
| 集成测试 | 验证模块间协作和数据流 | Vitest + idb-mock | 核心流程 100% |
| E2E 测试 | 验证用户完整操作流 | Playwright | 5 个核心页面 |

### 1.2 测试环境

| 环境 | 配置 | 用途 |
|------|------|------|
| 本地开发 | Node.js 18+, Chrome 最新版 | 单元测试、集成测试开发调试 |
| CI 环境 | GitHub Actions Ubuntu Runner | 自动化回归测试 |
| 浏览器矩阵 | Chrome 90+, Safari 15+, Firefox 90+, Edge 90+ | 兼容性测试 |
| 移动设备 | iPhone 12+ Safari, Android Chrome | 移动端兼容性测试 |

### 1.3 测试数据准备

| 数据类型 | 来源 | 说明 |
|----------|------|------|
| 内置词典 | `src/data/builtinDictionary.js` | 50+ 条佛学术语 |
| 测试经书 | `public/sutras/` | 心经（260 字）、金刚经（5000+ 字） |
| 用户词典 | 测试用例动态生成 | JSON/CSV/MDX 格式各准备一份 |
| 用户设置 | 测试 fixture | 包含三种主题、不同字号配置 |

---

## 2. 单元测试设计

### 2.1 Trie 树引擎测试

| 用例编号 | 测试目标 | 输入 | 预期输出 |
|----------|----------|------|----------|
| UT-TRIE-001 | 单词条插入与匹配 | insert("般若", 1), match("般若波罗蜜", 0) | { term: "般若", endIdx: 1, dictIds: [1] } |
| UT-TRIE-002 | 多词条插入与长词优先 | insert("般若", 1), insert("般若波罗蜜", 1), match("般若波罗蜜多", 0) | { term: "般若波罗蜜", endIdx: 4, dictIds: [1] } |
| UT-TRIE-003 | 跨词典词条匹配 | insert("般若", 1), insert("般若", 2), match("般若", 0) | dictIds: [1, 2] |
| UT-TRIE-004 | 无匹配返回 | insert("般若", 1), match("阿弥陀佛", 0) | null |
| UT-TRIE-005 | 批量插入性能 | insertBatch(10 万条) | 耗时 < 100ms |
| UT-TRIE-006 | 序列化与反序列化 | serialize() -> deserialize() | 匹配结果一致 |
| UT-TRIE-007 | 内存释放 | destroy() 后访问 root | root === null |
| UT-TRIE-008 | 最大匹配长度限制 | match(超长文本, 0, maxLen=20) | 最多扫描 20 个字符 |

### 2.2 高亮引擎测试

| 用例编号 | 测试目标 | 输入 | 预期输出 |
|----------|----------|------|----------|
| UT-HL-001 | 单词条高亮 | content="观自在菩萨行深般若波罗蜜多时", 匹配"般若" | 包含 `<span class="highlight">般若</span>` |
| UT-HL-002 | 多词条高亮 | content 含"般若"和"波罗蜜" | 两处分别高亮 |
| UT-HL-003 | 重叠词条处理 | 匹配"般若"和"般若波罗蜜" | 长词优先，仅高亮"般若波罗蜜" |
| UT-HL-004 | 过滤未启用词典 | 词条仅在 dictId=2 中，activeDictIds=[1] | 不高亮 |
| UT-HL-005 | 高亮颜色分配 | dictId=0,1,2,3 | 分别对应 4 种预定义颜色 |
| UT-HL-006 | 1000 字性能 | 1000 字经文 + 50 词条匹配 | 耗时 < 10ms |
| UT-HL-007 | 5000 字性能 | 5000 字经文 + 50 词条匹配 | 耗时 < 30ms |

### 2.3 IndexedDB 操作测试

| 用例编号 | 测试目标 | 操作 | 预期结果 |
|----------|----------|------|----------|
| UT-DB-001 | 数据库初始化 | initDB() | 13 张表全部创建 |
| UT-DB-002 | 词典写入与读取 | put(dict) -> get(id) | 数据一致 |
| UT-DB-003 | 复合主键查询 | get("1::般若") | 返回对应词条 |
| UT-DB-004 | 索引查询 | getByIndex("sutraId", 1) | 返回所有匹配记录 |
| UT-DB-005 | 事务批量写入 | 500 条/事务 | 全部写入成功 |
| UT-DB-006 | 版本迁移 | 从 v0 升级到 v1 | 13 张表自动创建 |
| UT-DB-007 | 存储配额监控 | quotaMonitor.check() | 返回已用/总配额比例 |
| UT-DB-008 | Blob 存储 | 存入 MDX 文件 Blob | 取出后大小和校验和一致 |

### 2.4 MDX 解析器测试

| 用例编号 | 测试目标 | 输入 | 预期结果 |
|----------|----------|------|----------|
| UT-MDX-001 | UTF-8 编码 MDX 解析 | UTF-8 编码 .mdx 文件 | 词条正确提取 |
| UT-MDX-002 | GBK 编码 MDX 解析 | GBK 编码 .mdx 文件 | 无乱码，词条正确 |
| UT-MDX-003 | Big5 编码 MDX 解析 | Big5 编码 .mdx 文件 | 繁中词条正确提取 |
| UT-MDX-004 | LZO 压缩 MDX 解析 | 含 LZO 压缩的 .mdx | 使用 lzo-wasm 解压后正确解析 |
| UT-MDX-005 | 恶意脚本检测 | 含 `<script>` 标签的 MDX | mdxScanner 返回扫描失败 |
| UT-MDX-006 | HTML 转 Markdown | 含 HTML 格式的释义 | turndown 转换为 Markdown |
| UT-MDX-007 | 小文件预解析 | 3MB .mdx 文件 | 全量预解析到 IndexedDB |
| UT-MDX-008 | 大文件懒加载 | 8MB .mdx 文件 | 仅解析索引，释义按需查询 |

### 2.5 Service 层测试

| 用例编号 | 测试目标 | 方法 | 预期结果 |
|----------|----------|------|----------|
| UT-SVC-001 | 词典列表查询 | listDictionaries() | 返回所有词典元数据 |
| UT-SVC-002 | 词条并行查询 | lookupTerms("般若", [1,2,3]) | 返回 3 个词典的释义 |
| UT-SVC-003 | LRU 缓存命中 | 第二次 lookupTerms 相同词条 | 直接返回缓存，不查 DB |
| UT-SVC-004 | 阅读进度保存 | saveProgress(1, 5, 1250) | IndexedDB 中记录正确数据 |
| UT-SVC-005 | 诵读记录 | recordRead(1, 260, 600) | statistics 表新增一条记录 |
| UT-SVC-006 | 连续天数计算 | getStreakDays() | 返回正确的连续天数 |
| UT-SVC-007 | 设置导出导入 | exportSettings() -> importSettings() | 设置完全恢复 |
| UT-SVC-008 | ServiceResult 格式 | 所有方法返回值 | 符合 { success, data/error } 格式 |

---

## 3. 集成测试设计

### 3.1 词典高亮集成流程

```mermaid
graph LR
    A["用户打开经书"] --> B["Service 加载章节内容"]
    B --> C["Highlighter 进行高亮处理"]
    C --> D["Trie 匹配词条"]
    D --> E["生成高亮 HTML"]
    E --> F["ReaderContent 渲染到 DOM"]
    F --> G["用户点击高亮词条"]
    G --> H["DictPopup 查询释义"]
    H --> I["多词典并行查询 IndexedDB"]
    I --> J["流式展示释义"]
```

| 用例编号 | 测试场景 | 操作步骤 | 预期结果 |
|----------|----------|----------|----------|
| IT-001 | 完整高亮查询流程 | 打开心经 -> 点击"般若"高亮 | DictPopup 显示释义，加载时间 < 200ms |
| IT-002 | 多词典并行查询 | 启用 3 个词典 -> 点击词条 | 3 个词典释义依次出现，无需等待全部完成 |
| IT-003 | 词典开关切换 | 关闭一个词典 -> 返回阅读页 | 该词典的词条不再高亮 |
| IT-004 | 笔记与释义合并 | 添加笔记 -> 再次查看同一词条 | 笔记显示在释义下方 |
| IT-005 | 主题切换影响 | 切换至暗色主题 -> 查看高亮和释义 | 颜色适配暗色模式，无闪烁 |

### 3.2 词典导入集成流程

| 用例编号 | 测试场景 | 操作步骤 | 预期结果 |
|----------|----------|----------|----------|
| IT-006 | JSON 词典导入 | 上传 .json 文件 | 导入成功，词典列表新增，高亮实时更新 |
| IT-007 | CSV 词典导入 | 上传 .csv 文件 | 导入成功，词条解析正确 |
| IT-008 | MDX 小文件导入 | 上传 3MB .mdx 文件 | 全量预解析，Trie 重建，高亮更新 |
| IT-009 | MDX 大文件导入 | 上传 8MB .mdx 文件 | 索引预解析，原文件缓存，按需查询 |
| IT-010 | 超大文件拒绝 | 上传 12MB 文件 | 提示文件过大，拒绝导入 |
| IT-011 | 恶意文件拦截 | 上传含 script 标签的 MDX | mdxScanner 拦截，提示安全风险 |
| IT-012 | 重复导入处理 | 上传同一词典第二次 | 创建新版本，保留版本历史 |
| IT-013 | 体检报告生成 | 导入完成后 | 显示词条数、重复数、格式检查结果 |

### 3.3 路由导航集成

| 用例编号 | 测试场景 | 操作步骤 | 预期结果 |
|----------|----------|----------|----------|
| IT-014 | 书架 -> 阅读器 | 点击经书卡片 | 跳转至阅读器，恢复上次阅读进度 |
| IT-015 | 阅读器 -> 词典管理 | 底部 Tab 切换 | 状态保持，返回阅读器时高亮正常 |
| IT-016 | 直接访问阅读器 URL | 浏览器输入 /reader/1 | 正确加载经书，无错误 |
| IT-017 | 404 路由 | 访问不存在的路径 | 重定向至书架首页 |
| IT-018 | 浏览器后退 | 阅读器 -> 后退 | 返回书架，阅读进度已保存 |

### 3.4 文件导入集成

| 用例编号 | 测试场景 | 操作步骤 | 预期结果 |
|----------|----------|----------|----------|
| IT-019 | 拖拽上传 | 拖拽 .mdx 文件到导入区域 | 识别文件，开始导入流程 |
| IT-020 | 文件选择器 | 点击选择文件 | 文件选择对话框打开，过滤显示支持的格式 |
| IT-021 | 导入进度展示 | 导入大型词典 | 实时显示进度百分比和已处理词条数 |
| IT-022 | 导入中断 | 导入过程中刷新页面 | 下次打开时导入未完成的事务回滚 |
| IT-023 | 多文件连续导入 | 连续导入 3 个词典 | 每个词典独立处理，互不影响 |

---

## 4. E2E 测试设计

### 4.1 核心用户旅程

#### 旅程一：首次使用

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 打开应用首页 | 显示书架页面，包含预置经书 |
| 2 | 点击心经卡片 | 跳转至阅读器，展示心经正文 |
| 3 | 查看高亮词条 | 佛学术语以不同颜色高亮 |
| 4 | 点击"般若"高亮 | 弹出释义窗口，显示释义 |
| 5 | 添加笔记 | 笔记保存成功 |
| 6 | 返回书架 | 心经卡片显示阅读进度 |

#### 旅程二：导入自定义词典

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 进入词典管理页 | 显示内置词典，开关开启 |
| 2 | 点击导入词典 | 文件选择器打开 |
| 3 | 选择 JSON 词典文件 | 导入开始，进度条展示 |
| 4 | 导入完成 | 显示体检报告，词典列表新增 |
| 5 | 返回阅读器 | 新词典词条开始高亮 |

#### 旅程三：主题切换与设置

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 进入设置页 | 显示所有设置项 |
| 2 | 切换至暗色主题 | 全站颜色切换，无闪烁 |
| 3 | 调整字号至 20px | 阅读器正文字号变为 20px |
| 4 | 导出设置 | 下载 JSON 配置文件 |
| 5 | 导入设置 | 设置恢复至导出时状态 |

### 4.2 E2E 测试用例

| 用例编号 | 名称 | 浏览器 | 预期结果 |
|----------|------|--------|----------|
| E2E-001 | 首次使用完整流程 | Chrome | 旅程一所有步骤通过 |
| E2E-002 | 词典导入流程 | Chrome | 旅程二所有步骤通过 |
| E2E-003 | 设置与主题切换 | Chrome | 旅程三所有步骤通过 |
| E2E-004 | 移动端响应式布局 | iPhone Safari | 三页面无横向滚动，触摸目标 >= 44px |
| E2E-005 | TTS 朗读功能 | Chrome | 点击朗读按钮后音频正常播放 |
| E2E-006 | 离线模式 | Chrome (断网) | 已加载数据可正常使用 |
| E2E-007 | 功德统计页面 | Chrome | 统计数据正确展示，图表渲染正常 |
| E2E-008 | 数据持久化 | Chrome | 刷新页面后所有设置和进度保持 |

---

## 5. 性能测试

### 5.1 性能指标矩阵

| 指标 | 目标值 | 测量方法 | 通过标准 |
|------|--------|----------|----------|
| FCP (首屏加载) | < 1s | Lighthouse | 3 次测试全部通过 |
| LCP (最大内容绘制) | < 2.5s | Lighthouse | 3 次测试全部通过 |
| TTI (可交互时间) | < 3.5s | Lighthouse | 3 次测试全部通过 |
| JS Bundle (gzip) | < 150KB | rollup-plugin-visualizer | 单次检查通过 |
| CSS Bundle (gzip) | < 30KB | rollup-plugin-visualizer | 单次检查通过 |
| Trie 构建 (10 万条) | < 100ms | performance.now() | 5 次测试平均值 |
| 高亮 (1000 字) | < 10ms | performance.now() | 5 次测试平均值 |
| 高亮 (5000 字) | < 30ms | performance.now() | 5 次测试平均值 |
| 释义加载 | < 200ms | performance.now() | 5 次测试平均值 |
| DOMPurify (短文本) | < 2ms | performance.now() | 5 次测试平均值 |
| DOMPurify (长文本) | < 15ms | performance.now() | 5 次测试平均值 |
| MDX 预解析 (<5MB) | < 1s | performance.now() | 3 次测试平均值 |
| Store 序列化 | < 500KB | performance.memory | 单次检查通过 |

### 5.2 内存测试场景

| 场景 | 常驻内存目标 | 峰值内存目标 | 测量方法 |
|------|-------------|-------------|----------|
| 仅内置词典 (50 条) | < 1MB | < 2MB | Chrome DevTools Memory |
| 内置 + 1 用户词典 (1 万条) | ~3MB | ~15MB | Chrome DevTools Memory |
| 内置 + 5 用户词典 (各 2 万条) | ~8MB | ~50MB | Chrome DevTools Memory |
| 极端场景 (10 万条) | ~25MB | ~200MB | Chrome DevTools Memory |

### 5.3 性能测试工具

| 工具 | 用途 | 集成方式 |
|------|------|----------|
| Lighthouse CI | 自动化性能审计 | GitHub Actions |
| rollup-plugin-visualizer | Bundle 大小分析 | `npm run build:analyze` |
| Chrome DevTools Performance | 运行时性能分析 | 手动测试 |
| Chrome DevTools Memory | 内存占用分析 | 手动测试 |

---

## 6. 兼容性测试

### 6.1 浏览器兼容性矩阵

| 浏览器 | 版本 | 桌面 | 移动 | 优先级 |
|--------|------|------|------|--------|
| Chrome | 90+ | 是 | Android | P0 |
| Safari | 15+ | macOS | iOS | P0 |
| Firefox | 90+ | 是 | - | P1 |
| Edge | 90+ | 是 | - | P1 |

### 6.2 功能兼容性矩阵

| 功能 | Chrome 90+ | Safari 15+ | Firefox 90+ | Edge 90+ |
|------|------------|------------|-------------|----------|
| IndexedDB | 完全支持 | 完全支持 | 完全支持 | 完全支持 |
| Web Worker | 完全支持 | 完全支持 | 完全支持 | 完全支持 |
| Web Speech API | 完全支持 | 部分支持 | 完全支持 | 完全支持 |
| Transferable Objects | 完全支持 | 完全支持 | 完全支持 | 完全支持 |
| CSS Custom Properties | 完全支持 | 完全支持 | 完全支持 | 完全支持 |
| IntersectionObserver | 完全支持 | 完全支持 | 完全支持 | 完全支持 |

### 6.3 设备兼容性矩阵

| 设备类型 | 分辨率 | 操作系统 | 浏览器 | 测试项 |
|----------|--------|----------|--------|--------|
| 手机 | 375x812 | iOS 15+ | Safari | 响应式布局、触摸交互、TTS |
| 手机 | 360x800 | Android 11+ | Chrome | 响应式布局、触摸交互、TTS |
| 平板 | 768x1024 | iPadOS 15+ | Safari | 网格布局、触摸交互 |
| 桌面 | 1920x1080 | Windows 10+ | Chrome/Edge | 完整功能 |
| 桌面 | 1440x900 | macOS 12+ | Safari/Chrome | 完整功能 |

### 6.4 iOS Worker 降级测试

| 用例编号 | 测试场景 | 操作步骤 | 预期结果 |
|----------|----------|----------|----------|
| COMP-001 | iOS 15.0-15.3 Worker 不可用 | 在 iOS 15.0 Safari 上导入词典 | 静默降级至主线程构建 Trie，用户无感知 |
| COMP-002 | Chrome 无痕模式 | 在 Chrome 无痕模式下使用 | 提示用户数据不持久，功能正常 |
| COMP-003 | 多标签页并发 | 同时打开 2 个标签页操作同一数据库 | versionchange 事件触发，提示用户刷新 |

---

## 7. 安全测试

### 7.1 XSS 防护测试

| 用例编号 | 测试场景 | 输入 | 预期结果 |
|----------|----------|------|----------|
| SEC-001 | 词典释义含 script 标签 | `<script>alert(1)</script>` | DOMPurify 移除 script，仅保留文本 |
| SEC-002 | 词典释义含 onerror 事件 | `<img onerror="alert(1)" src=x>` | DOMPurify 移除事件处理器 |
| SEC-003 | 词典释义含 javascript: 协议 | `<a href="javascript:alert(1)">` | DOMPurify 移除危险 href |
| SEC-004 | 用户笔记含 HTML | `<b>笔记</b><script>alert(1)</script>` | script 被移除，bold 标签保留 |

### 7.2 文件上传安全测试

| 用例编号 | 测试场景 | 输入 | 预期结果 |
|----------|----------|------|----------|
| SEC-005 | 非支持格式上传 | 上传 .exe 文件 | 拒绝，提示格式不支持 |
| SEC-006 | 超大文件上传 | 上传 15MB .mdx 文件 | 拒绝，提示超过 10MB 限制 |
| SEC-007 | MDX 含恶意脚本 | 上传含 `<script>` 的 .mdx | mdxScanner 拦截，拒绝导入 |
| SEC-008 | 文件名注入 | 上传 `../../../etc/passwd.json` | 文件名安全处理，不产生路径穿越 |

### 7.3 CSP 测试

| 用例编号 | 测试场景 | 预期结果 |
|----------|----------|----------|
| SEC-009 | 加载外部脚本 | CSP 阻止，控制台报错 |
| SEC-010 | 加载外部样式 | CSP 阻止，控制台报错 |
| SEC-011 | iframe 嵌入 | CSP 阻止（frame-src: none） |
| SEC-012 | 内联对象加载 | CSP 阻止（object-src: none） |

---

## 8. 测试执行计划

### 8.1 Phase 1：单元测试（与开发同步）

| 模块 | 优先级 | 完成标准 |
|------|--------|----------|
| Trie 树引擎 | P0 | 所有 UT-TRIE-* 用例通过 |
| 高亮引擎 | P0 | 所有 UT-HL-* 用例通过 |
| IndexedDB 操作 | P0 | 所有 UT-DB-* 用例通过 |
| MDX 解析器 | P1 | 所有 UT-MDX-* 用例通过 |
| Service 层 | P0 | 所有 UT-SVC-* 用例通过 |

### 8.2 Phase 2：集成测试（Phase 1 完成后）

| 模块 | 优先级 | 完成标准 |
|------|--------|----------|
| 词典高亮集成 | P0 | IT-001 ~ IT-005 通过 |
| 词典导入集成 | P0 | IT-006 ~ IT-013 通过 |
| 路由导航集成 | P1 | IT-014 ~ IT-018 通过 |
| 文件导入集成 | P1 | IT-019 ~ IT-023 通过 |

### 8.3 Phase 3：E2E 测试（功能完成后）

| 旅程 | 优先级 | 完成标准 |
|------|--------|----------|
| 首次使用 | P0 | E2E-001 通过 |
| 词典导入 | P0 | E2E-002 通过 |
| 设置与主题 | P1 | E2E-003 通过 |
| 移动端 | P0 | E2E-004 通过 |
| TTS | P2 | E2E-005 通过 |
| 离线 | P1 | E2E-006 通过 |
| 统计 | P2 | E2E-007 通过 |
| 持久化 | P0 | E2E-008 通过 |

### 8.4 Phase 4：性能与兼容性测试（上线前）

| 类别 | 完成标准 |
|------|----------|
| 性能测试 | 所有指标达到目标值 |
| 兼容性测试 | P0 浏览器全部通过，P1 浏览器无阻塞性 Bug |
| 安全测试 | 所有 SEC-* 用例通过 |

---

## 9. 测试通过标准

| 级别 | 标准 |
|------|------|
| 阻塞 (Blocker) | P0 用例 100% 通过，无未解决的 P0 Bug |
| 发布 (Release) | P0 + P1 用例 95% 通过，无 P0/P1 未解决 Bug |
| 优秀 (Excellent) | 全部用例 100% 通过，性能指标全部达标 |

---

*文档版本: v1.0*  
*最后更新: 2026-05-03*
