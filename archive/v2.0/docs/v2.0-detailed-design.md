# 般若佛经阅读器 v2.0 项目方案

> **版本**：v2.0.1-final  
> **日期**：2026-05-02  
> **状态**：待评审  
> **基于**：52 个调研任务结论（T-01 ~ T-52）+ awesome-design-md 设计系统调研

---

## 1. 项目概述

### 1.1 项目定位

般若佛经阅读器是一款**禅意极简风格**的佛教经文阅读与词典查询 Web 应用，专注于：

- **经文诵读**：提供流畅的中文经文阅读体验，支持诵读模式
- **词典高亮**：多词典词条实时高亮匹配，点击即可查看释义
- **用户笔记**：在阅读过程中记录个人理解和感悟
- **完全离线**：所有数据存储在浏览器 IndexedDB 中，无需后端

### 1.2 核心特性

| 特性 | 说明 |
|------|------|
| **禅意设计** | 宋体优先、暖色调宣纸背景、克制优雅的动画 |
| **多词典高亮** | 支持内置词典 + 用户上传 MDX/JSON/CSV 词典 |
| **流式释义** | 多词典并行查询，先返回先渲染，无需等待 |
| **完全离线** | IndexedDB 存储，PWA 支持，无网络也可使用 |
| **移动端优先** | 响应式设计，适配手机/平板/PC |

### 1.3 技术选型依据

经过 52 个调研任务的深度分析，核心技术栈选定如下：

| 层 | 技术 | 选型依据 |
|----|------|----------|
| **框架** | Vue 3 + Composition API | T-01：生态成熟、组件拆分灵活 |
| **构建** | Vite 5 | 原生 ESM、冷启动快、插件生态完善 |
| **UI** | Vant 4 | T-03：移动端优化、91% 组件覆盖 |
| **状态** | Pinia | T-01：TypeScript 友好、按需注册 |
| **存储** | IndexedDB + idb (~3KB) | T-04：容量大、10 万词条读写 < 5ms |
| **路由** | Vue Router 4 | 官方配套、history 模式 |
| **渲染** | markdown-it + DOMPurify | T-07/T-14：插件生态、SSR 兼容、XSS 防护 |
| **转换** | turndown | T-09：浏览器端 HTML→Markdown 唯一方案 |
| **词典** | mdict-js (v10.0.1 MIT) | T-06：支持 UTF-8/GBK/Big5、lookup 20K ops/sec |
| **压缩** | lzo-wasm | T-06：WASM 解压 250-400MB/s |
| **图标** | Lucide Icons | T-49：线性风格符合禅意，禁用 Emoji |

---

## 2. 架构设计

### 2.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                        表现层 (UI)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ Bookshelf│  │  Reader  │  │   Dict   │  │Settings │ │
│  │  书架页   │  │  阅读页   │  │ 词典管理  │  │ 设置页   │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
├─────────────────────────────────────────────────────────┤
│                   组件层 (Components)                    │
│  ReaderContent / DictPopup / NoteEditor / TOC / Highlight│
├─────────────────────────────────────────────────────────┤
│                    状态层 (Pinia)                        │
│  sutraStore / dictStore / readerStore / settingStore /   │
│  statsStore                                              │
├─────────────────────────────────────────────────────────┤
│                  服务层 (Service)                        │
│  ISutraService / IDictService / IProgressService /       │
│  IStatsService / ITTSService / ISettingService           │
├─────────────────────────────────────────────────────────┤
│                  存储层 (Storage)                        │
│  storage/db.js / storage/sutraStore.js /                 │
│  storage/dictStore.js / storage/fallback.js              │
├─────────────────────────────────────────────────────────┤
│                   引擎层 (Engine)                        │
│  engine/trie/ / engine/highlighter.js / engine/mdxParser│
│  engine/markdownRenderer.js / engine/pinyin.js           │
├─────────────────────────────────────────────────────────┤
│                   Web Worker 层                          │
│  worker/trieBuilder.js / worker/mdxParser.js             │
└─────────────────────────────────────────────────────────┘
```

### 2.2 核心设计原则

| 原则 | 说明 |
|------|------|
| **接口与实现分离** | Service 层定义 TypeScript 接口，IndexedDB 和未来 REST API 分别实现 |
| **大型数据不入库** | 章节内容、释义全文等大数据不放入 Pinia Store，由 Service 层按需加载 |
| **按需懒加载** | 词典索引按需加载，MDX 按大小分级处理，经书分块存储 |
| **纯前端优先** | 所有数据存储在 IndexedDB，预留后端 API 切换能力 |
| **安全四层防护** | 文件验证 → MDX 扫描 → DOMPurify 净化 → CSP 拦截 |

### 2.3 项目目录结构

```
buddhist-reader/
├── public/
│   ├── sutras/                    # 经书原始文件（TXT 格式）
│   │   ├── heart-sutra.txt
│   │   └── diamond-sutra.txt
│   └── favicon.ico
├── src/
│   ├── assets/                    # 静态资源
│   │   ├── fonts/                 # 字体（按需加载）
│   │   │   ├── noto-serif-sc/
│   │   │   └── noto-sans/
│   │   └── icons/                 # Lucide SVG 图标
│   ├── components/                # Vue 组件
│   │   ├── bookshelf/             # 书架相关
│   │   │   ├── BookshelfList.vue
│   │   │   └── SutraCard.vue
│   │   ├── reader/                # 阅读相关
│   │   │   ├── ReaderHeader.vue
│   │   │   ├── ReaderContent.vue
│   │   │   ├── ReaderTOC.vue
│   │   │   ├── ReaderProgress.vue
│   │   │   └── ReaderFooter.vue
│   │   ├── dict/                  # 词典相关
│   │   │   ├── DictPopup.vue
│   │   │   ├── DictManager.vue
│   │   │   ├── DictList.vue
│   │   │   └── DictImporter.vue
│   │   ├── common/                # 通用组件
│   │   │   ├── MarkdownRenderer.vue
│   │   │   ├── NoteEditor.vue
│   │   │   ├── SearchBar.vue
│   │   │   ├── ThemeSwitcher.vue
│   │   │   └── LoadingSpinner.vue
│   │   └── layout/                # 布局组件
│   │       ├── AppHeader.vue
│   │       ├── AppSidebar.vue
│   │       └── AppTabBar.vue
│   ├── engine/                    # 核心引擎
│   │   ├── trie/                  # Trie 树引擎
│   │   │   ├── TrieNode.js
│   │   │   ├── TrieBuilder.js
│   │   │   └── TrieMatcher.js
│   │   ├── highlighter.js         # 高亮渲染引擎
│   │   ├── mdxParser.js           # MDX 解析器
│   │   ├── markdownRenderer.js    # Markdown 渲染器
│   │   ├── pinyin.js              # 拼音处理
│   │   └── formatter.js           # 释义格式化
│   ├── services/                  # Service 层（接口 + 实现）
│   │   ├── interfaces/            # TypeScript 接口定义
│   │   │   ├── ISutraService.ts
│   │   │   ├── IDictService.ts
│   │   │   ├── IProgressService.ts
│   │   │   ├── IStatsService.ts
│   │   │   ├── ITTSService.ts
│   │   │   └── ISettingService.ts
│   │   ├── factory.ts             # Service 工厂
│   │   └── impl/                  # IndexedDB 实现
│   │       ├── SutraServiceLocal.js
│   │       ├── DictServiceLocal.js
│   │       ├── ProgressServiceLocal.js
│   │       ├── StatsServiceLocal.js
│   │       ├── TTSServiceLocal.js
│   │       └── SettingServiceLocal.js
│   ├── stores/                    # Pinia 状态管理
│   │   ├── sutra.js               # 经书状态（元数据、目录）
│   │   ├── dict.js                # 词典状态（开关、健康度）
│   │   ├── reader.js              # 阅读器状态（当前章节、进度）
│   │   ├── setting.js             # 用户设置（主题、字体、高亮）
│   │   └── stats.js               # 统计状态（功德、里程碑）
│   ├── storage/                   # IndexedDB 存储层
│   │   ├── db.js                  # 数据库初始化（13 张表）
│   │   ├── migration.js           # 版本迁移脚本
│   │   ├── sutraStore.js          # 经书表操作
│   │   ├── dictStore.js           # 词典表操作
│   │   ├── noteStore.js           # 笔记表操作
│   │   ├── progressStore.js       # 进度表操作
│   │   ├── statsStore.js          # 统计表操作
│   │   ├── settingStore.js        # 设置表操作
│   │   ├── fileCacheStore.js      # 文件缓存操作
│   │   ├── quotaMonitor.js        # 存储配额监控
│   │   └── fallback.js            # 降级策略（废弃三级降级）
│   ├── utils/                     # 工具函数
│   │   ├── validateFile.js        # 文件类型/大小验证
│   │   ├── mdxScanner.js          # MDX 恶意内容扫描
│   │   ├── logger.js              # 轻量日志模块
│   │   ├── errorHandler.js        # 错误分类与处理
│   │   └── idbErrorHandler.js     # IndexedDB 错误处理
│   ├── data/                      # 静态数据
│   │   ├── builtinDictionary.js   # 内置词典（50+ 词条）
│   │   ├── sutraManifest.js       # 经书清单
│   │   └── pronunciationMap.js    # 读音映射（16 条）
│   ├── worker/                    # Web Worker
│   │   ├── trieBuilder.worker.js  # Trie 构建 Worker
│   │   └── mdxParser.worker.js    # MDX 解析 Worker
│   ├── styles/                    # 样式
│   │   ├── tokens.css             # 禅意 Token 系统（~91 个变量）
│   │   ├── vant-override.css      # Vant 4 禅意覆盖
│   │   ├── base.css               # 基础重置
│   │   └── themes/                # 主题样式
│   │       ├── day.css
│   │       ├── dark.css
│   │       └── eye-care.css
│   ├── router/                    # 路由配置
│   │   └── index.js
│   ├── pages/                     # 页面组件
│   │   ├── Bookshelf.vue
│   │   ├── Reader.vue
│   │   ├── DictManager.vue
│   │   ├── Settings.vue
│   │   └── Stats.vue
│   ├── App.vue
│   └── main.js
├── worker/                        # Worker 配置（Vite 外部）
│   └── *.worker.js
├── index.html
├── vite.config.js
├── vercel.json
├── package.json
└── README.md
```

---

## 3. 数据模型

### 3.1 数据库概览

| 属性 | 值 |
|------|-----|
| **数据库名** | `buddhist-reader-v2` |
| **当前版本** | 1 |
| **存储库** | idb (~3KB gzip) |
| **表数量** | 13 |
| **预估总容量** | 50-200MB（视词典数量） |

### 3.2 13 张表完整定义

#### 1. dictionaries — 词典元数据

```javascript
{
  keyPath: 'id',             // 自增 ID
  indexes: [
    { name: 'authority', keyPath: 'authority' },  // 'official' | 'personal'
    { name: 'isActive', keyPath: 'isActive' }     // boolean
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | number | 自增主键 | 1 |
| name | string | 词典名称 | "佛教大辞典" |
| type | string | 词典类型 | 'mdx' \| 'json' \| 'csv' \| 'builtin' |
| authority | string | 权威性 | 'official' \| 'personal' |
| isActive | boolean | 开关状态 | true |
| entryCount | number | 词条数 | 12500 |
| mdxStrategy | string | MDX 策略 | 'parsed' \| 'index' \| 'direct' \| null |
| mdxVersion | string | MDX 版本 | '2.0' \| '1.2' \| null |
| createdAt | ISO8601 | 创建时间 | "2026-05-02T10:00:00Z" |
| updatedAt | ISO8601 | 更新时间 | "2026-05-02T10:00:00Z" |

#### 2. dict_entries — 词典条目

```javascript
{
  keyPath: 'key',             // 复合主键: `${dictId}::${term}`
  indexes: [
    { name: 'term', keyPath: 'term' },
    { name: 'dictId', keyPath: 'dictId' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| key | string | 复合主键 | "1::般若" |
| term | string | 词条文本 | "般若" |
| dictId | number | 词典 ID | 1 |
| definition | string | 释义内容（Markdown 或 HTML） | "梵语 Prajñā 的音译..." |
| format | string | 存储格式 | 'markdown' \| 'html' |
| pinyin | string | 拼音 | "bō rě" |
| category | string | 分类 | 'term' \| 'sanskrit' \| 'name' |
| checksum | string | 内容校验和（SHA-256） | "a1b2c3..." |
| updatedAt | ISO8601 | 更新时间 | "2026-05-02T10:00:00Z" |

#### 3. dict_term_lookup — 词条索引（Trie 构建数据源）

```javascript
{
  keyPath: 'key',             // `${dictId}::${term}`
  indexes: [
    { name: 'dictId', keyPath: 'dictId' },
    { name: 'chunkId', keyPath: 'chunkId' }  // 仅大词典使用
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| key | string | 复合主键 | "1::般若" |
| term | string | 词条文本 | "般若" |
| dictId | number | 词典 ID | 1 |
| chunkId | string \| null | 分块 ID（仅 >5MB 词典） | "chunk_001" \| null |

**重要**：此表**不包含 definition**，仅存储 term + dictId + chunkId 用于 Trie 构建和快速定位。

#### 4. dictionary_chunks — 词典分块数据

```javascript
{
  keyPath: 'id',             // `${dictId}::${chunkId}`
  indexes: [
    { name: 'dictId', keyPath: 'dictId' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | string | 主键 | "1::chunk_001" |
| dictId | number | 词典 ID | 1 |
| chunkId | string | 分块 ID | "chunk_001" |
| entries | object[] | 该分块的词条数据 | [{term, definition, ...}] |
| size | number | 分块大小（字节） | 2097152 (2MB) |

#### 5. dictionary_versions — 词典版本历史

```javascript
{
  keyPath: 'id',
  indexes: [
    { name: 'dictId', keyPath: 'dictId' },
    { name: 'version', keyPath: 'version' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | number | 自增主键 | 1 |
| dictId | number | 词典 ID | 1 |
| version | number | 版本号（递增） | 1 |
| uploadTime | ISO8601 | 上传时间 | "2026-05-02T10:00:00Z" |
| entryCount | number | 词条数 | 12500 |
| checksum | string | 文件校验和 | "a1b2c3..." |
| notes | string | 版本备注 | "初始导入" |

#### 6. sutras — 经书元数据

```javascript
{
  keyPath: 'id',
  indexes: [
    { name: 'slug', keyPath: 'slug', unique: true },
    { name: 'category', keyPath: 'category' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | number | 自增主键 | 1 |
| title | string | 经书名称 | "般若波罗蜜多心经" |
| slug | string | 唯一标识 | "heart-sutra" |
| category | string | 分类 | 'prajna' \| 'pure-land' \| 'chan' |
| translator | string | 译者 | "玄奘" |
| chapterCount | number | 章节数 | 1 |
| totalChars | number | 总字数 | 260 |
| coverUrl | string \| null | 封面 URL | null |
| createdAt | ISO8601 | 创建时间 | "2026-05-02T10:00:00Z" |

#### 7. sutra_chapters — 经书章节

```javascript
{
  keyPath: 'id',
  indexes: [
    { name: 'sutraId', keyPath: 'sutraId' },
    { name: 'chapterIndex', keyPath: 'chapterIndex' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | number | 自增主键 | 1 |
| sutraId | number | 经书 ID | 1 |
| chapterIndex | number | 章节序号 | 1 |
| title | string | 章节标题 | "观自在菩萨行深般若..." |
| content | string | 章节正文 | "观自在菩萨，行深般若..." |
| wordCount | number | 字数 | 260 |

#### 8. user_notes — 用户笔记

```javascript
{
  keyPath: 'id',
  indexes: [
    { name: 'entryKey', keyPath: 'entryKey' },
    { name: 'sutraId', keyPath: 'sutraId' },
    { name: 'updatedAt', keyPath: 'updatedAt' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | number | 自增主键 | 1 |
| entryKey | string | 关联词条 | "1::般若" |
| sutraId | number \| null | 关联经书 | 1 \| null |
| originalChecksum | string | 原始释义校验和 | "a1b2c3..." |
| noteContent | string | 笔记内容（Markdown） | "般若无翻，意为智慧..." |
| noteFormat | string | 笔记格式 | 'markdown' |
| isEncrypted | boolean | 是否加密 | false |
| createdAt | ISO8601 | 创建时间 | "2026-05-02T10:00:00Z" |
| updatedAt | ISO8601 | 更新时间 | "2026-05-02T10:00:00Z" |

#### 9. user_highlights — 用户高亮标记

```javascript
{
  keyPath: 'id',
  indexes: [
    { name: 'sutraId', keyPath: 'sutraId' },
    { name: 'term', keyPath: 'term' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | number | 自增主键 | 1 |
| sutraId | number | 经书 ID | 1 |
| term | string | 高亮词条 | "般若" |
| color | string | 高亮颜色 | '#FFF3CD' |
| note | string | 高亮备注 | "" |
| createdAt | ISO8601 | 创建时间 | "2026-05-02T10:00:00Z" |

#### 10. reading_progress — 阅读进度

```javascript
{
  keyPath: 'sutraId',
  indexes: []
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| sutraId | number | 经书 ID（主键） | 1 |
| chapterIndex | number | 当前章节 | 5 |
| scrollPosition | number | 滚动位置（像素） | 1250 |
| readPercentage | number | 阅读进度（0-100） | 45.5 |
| lastReadAt | ISO8601 | 最后阅读时间 | "2026-05-02T10:00:00Z" |

#### 11. settings — 用户设置

```javascript
{
  keyPath: 'key',
  indexes: []
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| key | string | 设置键 | "theme" |
| value | any | 设置值 | "day" |
| category | string | 分类 | 'display' \| 'reader' \| 'dict' \| 'system' |
| updatedAt | ISO8601 | 更新时间 | "2026-05-02T10:00:00Z" |

**设置项清单**：

| key | 默认值 | 说明 |
|-----|--------|------|
| theme | 'day' | 主题：'day' \| 'dark' \| 'eye-care' |
| fontSize | 18 | 正文字号（px） |
| lineHeight | 1.8 | 行高倍数 |
| pageMargin | 16 | 页边距（px） |
| highlightMode | 'background' | 高亮模式：'background' \| 'underline' |
| displayMode | 'expandFirst' | 释义展示：'collapse' \| 'expandFirst' \| 'expandAll' |
| ttsRate | 1.0 | TTS 语速（0.5-2.0） |
| ttsEnabled | true | TTS 开关 |

#### 12. statistics — 诵读统计

```javascript
{
  keyPath: 'id',
  indexes: [
    { name: 'sutraId', keyPath: 'sutraId' },
    { name: 'date', keyPath: 'date' },
    { name: 'sutraId_date', keyPath: ['sutraId', 'date'] }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| id | number | 自增主键 | 1 |
| sutraId | number | 经书 ID | 1 |
| date | string | 日期（YYYY-MM-DD） | "2026-05-02" |
| readCount | number | 诵读次数 | 3 |
| readChars | number | 诵读字数 | 780 |
| readDuration | number | 诵读时长（秒） | 600 |
| streakDays | number | 连续天数 | 7 |

#### 13. file_cache — 文件缓存

```javascript
{
  keyPath: 'key',
  indexes: [
    { name: 'type', keyPath: 'type' },
    { name: 'expiresAt', keyPath: 'expiresAt' }
  ]
}
```

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| key | string | 主键 | "mdx_1_original" |
| type | string | 文件类型 | 'mdx_original' \| 'mdd_resource' |
| data | Blob | 文件二进制数据 | ArrayBuffer |
| size | number | 文件大小（字节） | 5242880 (5MB) |
| createdAt | ISO8601 | 创建时间 | "2026-05-02T10:00:00Z" |
| expiresAt | ISO8601 | 过期时间 | "2027-05-02T10:00:00Z" |

### 3.3 数据库版本迁移

```javascript
// storage/db.js
import { openDB } from 'idb';

export async function initDB() {
  return openDB('buddhist-reader-v2', 1, {
    upgrade(db, oldVersion, newVersion, transaction) {
      if (oldVersion < 1) {
        // 创建 13 张表
        createTable(db, 'dictionaries', { keyPath: 'id', autoIncrement: true });
        createTable(db, 'dict_entries', { keyPath: 'key' });
        createTable(db, 'dict_term_lookup', { keyPath: 'key' });
        createTable(db, 'dictionary_chunks', { keyPath: 'id' });
        createTable(db, 'dictionary_versions', { keyPath: 'id', autoIncrement: true });
        createTable(db, 'sutras', { keyPath: 'id', autoIncrement: true });
        createTable(db, 'sutra_chapters', { keyPath: 'id', autoIncrement: true });
        createTable(db, 'user_notes', { keyPath: 'id', autoIncrement: true });
        createTable(db, 'user_highlights', { keyPath: 'id', autoIncrement: true });
        createTable(db, 'reading_progress', { keyPath: 'sutraId' });
        createTable(db, 'settings', { keyPath: 'key' });
        createTable(db, 'statistics', { keyPath: 'id', autoIncrement: true });
        createTable(db, 'file_cache', { keyPath: 'key' });
      }
    }
  });
}

function createTable(db, name, options) {
  const store = db.createObjectStore(name, options);
  // 根据表定义添加索引
  addIndexes(store, name);
}
```

---

## 4. 模块设计

### 4.1 Service 层接口定义

#### ServiceResult 统一返回格式

```typescript
// services/interfaces/types.ts

export interface ServiceError {
  code: string;           // 错误码
  message: string;        // 用户友好消息
  detail?: string;        // 技术详情（开发环境）
}

export interface ServiceResult<T> {
  success: boolean;
  data?: T;
  error?: ServiceError;
}

// 成功/失败工厂函数
export function ok<T>(data: T): ServiceResult<T> {
  return { success: true, data };
}

export function fail<T>(code: string, message: string, detail?: string): ServiceResult<T> {
  return { success: false, error: { code, message, detail } };
}
```

#### IDictService — 词典服务

```typescript
// services/interfaces/IDictService.ts

export interface IDictService {
  // 词典管理
  listDictionaries(): Promise<ServiceResult<Dictionary[]>>;
  getDictionary(id: number): Promise<ServiceResult<Dictionary>>;
  toggleDictionary(id: number, isActive: boolean): Promise<ServiceResult<void>>;
  deleteDictionary(id: number): Promise<ServiceResult<void>>;

  // 词典导入
  importDictionary(file: File): Promise<ServiceResult<ImportProgress>>;
  validateDictionary(file: File): Promise<ServiceResult<ValidationResult>>;

  // 词条查询
  lookupTerms(term: string, activeDictIds?: number[]): Promise<ServiceResult<TermDefinition[]>>;
  getTermDetail(dictId: number, term: string): Promise<ServiceResult<TermDefinition>>;

  // 索引管理
  buildTrieIndex(dictId: number): Promise<ServiceResult<void>>;
  getDictHealthReport(dictId: number): Promise<ServiceResult<DictHealthReport>>;
}

export interface Dictionary {
  id: number;
  name: string;
  type: 'mdx' | 'json' | 'csv' | 'builtin';
  authority: 'official' | 'personal';
  isActive: boolean;
  entryCount: number;
  mdxStrategy: 'parsed' | 'index' | 'direct' | null;
  createdAt: string;
  updatedAt: string;
}

export interface TermDefinition {
  dictId: number;
  dictName: string;
  term: string;
  definition: string;
  format: 'markdown' | 'html';
  pinyin?: string;
  category?: string;
}
```

#### ISutraService — 经书服务

```typescript
// services/interfaces/ISutraService.ts

export interface ISutraService {
  listSutras(): Promise<ServiceResult<Sutra[]>>;
  getSutra(id: number): Promise<ServiceResult<Sutra>>;
  getChapter(sutraId: number, chapterIndex: number): Promise<ServiceResult<Chapter>>;
  searchSutra(sutraId: number, keyword: string): Promise<ServiceResult<SearchResult[]>>;
  importSutra(file: File): Promise<ServiceResult<ImportProgress>>;
}
```

#### 其他 Service 接口

```typescript
export interface IProgressService {
  getProgress(sutraId: number): Promise<ServiceResult<ReadingProgress>>;
  saveProgress(sutraId: number, chapterIndex: number, scrollPosition: number): Promise<ServiceResult<void>>;
}

export interface IStatsService {
  recordRead(sutraId: number, chars: number, duration: number): Promise<ServiceResult<void>>;
  getStats(sutraId?: number, startDate?: string, endDate?: string): Promise<ServiceResult<StatsSummary>>;
  getStreakDays(): Promise<ServiceResult<number>>;
}

export interface ITTSService {
  init(): Promise<ServiceResult<void>>;
  speak(text: string, options?: TTSOptions): Promise<ServiceResult<void>>;
  stop(): Promise<ServiceResult<void>>;
  pause(): Promise<ServiceResult<void>>;
  resume(): Promise<ServiceResult<void>>;
  getVoices(lang?: string): Promise<ServiceResult<SpeechVoice[]>>;
}

export interface ISettingService {
  getSetting(key: string): Promise<ServiceResult<any>>;
  setSetting(key: string, value: any): Promise<ServiceResult<void>>;
  getSettingsByCategory(category: string): Promise<ServiceResult<Record<string, any>>>;
  exportSettings(): Promise<ServiceResult<Record<string, any>>>;
  importSettings(settings: Record<string, any>): Promise<ServiceResult<void>>;
}
```

### 4.2 Service 工厂模式

```javascript
// services/factory.js

let services = null;

export function initServices(mode = 'local') {
  if (mode === 'api') {
    // 未来后端 API 实现
    services = {
      sutra: new SutraServiceApi(),
      dict: new DictServiceApi(),
      progress: new ProgressServiceApi(),
      stats: new StatsServiceApi(),
      tts: new TTSServiceApi(),
      setting: new SettingServiceApi()
    };
  } else {
    // 默认 IndexedDB 实现
    services = {
      sutra: new SutraServiceLocal(),
      dict: new DictServiceLocal(),
      progress: new ProgressServiceLocal(),
      stats: new StatsServiceLocal(),
      tts: new TTSServiceLocal(),
      setting: new SettingServiceLocal()
    };
  }
  return services;
}

export function getServices() {
  if (!services) {
    return initServices();
  }
  return services;
}
```

### 4.3 Trie 引擎设计

#### Trie 数据结构

```javascript
// engine/trie/TrieNode.js

export class TrieNode {
  constructor() {
    this.children = new Map();   // char -> TrieNode
    this.isEnd = false;          // 是否是词条结尾
    this.dictIds = new Set();    // 包含此词条的词典 ID
  }
}
```

#### Trie 构建器（Worker 中使用）

```javascript
// engine/trie/TrieBuilder.js

export class TrieBuilder {
  constructor() {
    this.root = new TrieNode();
  }

  insert(term, dictId) {
    let node = this.root;
    for (const char of term) {
      if (!node.children.has(char)) {
        node.children.set(char, new TrieNode());
      }
      node = node.children.get(char);
    }
    node.isEnd = true;
    node.dictIds.add(dictId);
  }

  // 批量插入（用于 Worker 构建）
  insertBatch(terms) {
    for (const { term, dictId } of terms) {
      this.insert(term, dictId);
    }
  }

  // 序列化（用于 Worker → 主线程传输）
  serialize() {
    // 将 Trie 转换为并行数组结构
    const chars = [];
    const isEnds = [];
    const dictIdsList = [];
    const childIndices = [];
    // BFS 遍历序列化...
    return { chars, isEnds, dictIdsList, childIndices };
  }

  destroy() {
    this.root = null;  // 释放内存
  }
}
```

#### Trie 匹配器（主线程中使用）

```javascript
// engine/trie/TrieMatcher.js

export class TrieMatcher {
  constructor(serializedData) {
    this.deserialize(serializedData);
  }

  // 贪心长词优先匹配
  match(text, startIdx, maxLen = 20) {
    let node = this.root;
    let lastEnd = -1;
    let lastDictIds = new Set();

    for (let i = startIdx; i < Math.min(startIdx + maxLen, text.length); i++) {
      const char = text[i];
      if (!node.children.has(char)) break;

      node = node.children.get(char);
      if (node.isEnd) {
        lastEnd = i;
        lastDictIds = new Set(node.dictIds);
      }
    }

    if (lastEnd >= 0) {
      return {
        term: text.slice(startIdx, lastEnd + 1),
        endIdx: lastEnd,
        dictIds: Array.from(lastDictIds)
      };
    }
    return null;
  }
}
```

### 4.4 高亮渲染引擎

```javascript
// engine/highlighter.js

import { TrieMatcher } from './trie/TrieMatcher.js';

export class Highlighter {
  constructor(matcher) {
    this.matcher = matcher;
  }

  // 对经文内容进行高亮处理
  highlight(content, activeDictIds) {
    const segments = [];
    let idx = 0;

    while (idx < content.length) {
      const match = this.matcher.match(content, idx);

      if (match && this.hasActiveDict(match.dictIds, activeDictIds)) {
        // 添加高亮段
        segments.push({
          type: 'highlight',
          term: match.term,
          dictIds: match.dictIds,
          text: match.term
        });
        idx = match.endIdx + 1;
      } else {
        // 添加普通文本段
        segments.push({
          type: 'text',
          text: content[idx]
        });
        idx++;
      }
    }

    return this.renderSegments(segments);
  }

  // 渲染为 HTML 字符串
  renderSegments(segments) {
    let html = '';
    for (const seg of segments) {
      if (seg.type === 'highlight') {
        const color = this.getHighlightColor(seg.dictIds[0]);
        html += `<span class="highlight" data-term="${seg.term}" data-dict-ids="${seg.dictIds.join(',')}" style="background:${color}">${seg.text}</span>`;
      } else {
        html += seg.text;
      }
    }
    return html;
  }

  hasActiveDict(dictIds, activeDictIds) {
    return dictIds.some(id => activeDictIds.includes(id));
  }

  getHighlightColor(dictId) {
    const colors = ['#FFF3CD', '#D1ECF1', '#D4EDDA', '#E2D5F1'];
    return colors[dictId % colors.length];
  }
}
```

---

## 5. 核心流程

### 5.1 高亮匹配流程

```
用户点击高亮词条
       │
       ▼
  获取词条文本 (term)
       │
       ▼
  DictService.lookupTerms(term, activeDictIds)
       │
       ├── 内存 LRU 缓存命中? → 直接返回
       │         │
       │         ▼
       │    DOMPurify 净化
       │         │
       │         ▼
       │    返回释义
       │
       ▼
  IndexedDB 查询 dict_entries
       │
       ▼
  命中缓存? 写入 LRU
       │
       ▼
  多词典并行查询（Promise.all）
       │
       ▼
  流式渲染（先返回的先展示）
       │
       ▼
  DictPopup 展示释义（3 种模式）
```

### 5.2 MDX 词典导入流程

```
用户选择 MDX 文件
       │
       ▼
  1. 文件验证
     - 扩展名 .mdx
     - 大小 <= 10MB
       │
       ▼
  2. 恶意内容扫描（mdxScanner）
     - 检查 script 标签
     - 检查 onerror/onload 事件
     - 检查 javascript: 协议
       │
       ▼
  3. 根据大小选择策略
     ├── <5MB  → 全量预解析模式
     ├── 5-10MB → 索引预解析 + 释义懒加载
     └── >10MB  → 拒绝导入（提示用户）
       │
       ▼
  4. Worker 中解析 MDX
     - mdict-js 解析
     - lzo-wasm 解压（如需要）
     - turndown HTML→Markdown 转换
       │
       ▼
  5. 写入 IndexedDB
     - dictionaries 表（元数据）
     - dict_entries 表（词条）
     - dict_term_lookup 表（索引）
     - 每 500 条一个事务
       │
       ▼
  6. 构建 Trie 索引
     - Worker 中构建 Trie
     - 序列化传输到主线程
     - 更新 dictStore
       │
       ▼
  7. 完成，高亮实时更新
```

### 5.3 Trie 构建流程

```
词典导入完成 / 开关切换
       │
       ▼
  从 dict_term_lookup 表读取索引数据
       │
       ▼
  发送到 Worker（postMessage + Transferable）
       │
       ▼
  Worker 中构建 Trie
  - TrieBuilder.insertBatch(terms)
  - 按词典 ID 分层构建
       │
       ▼
  Trie 序列化（并行数组结构）
       │
       ▼
  传回主线程
       │
       ▼
  TrieMatcher 反序列化
       │
       ▼
  更新 dictStore.activeTrie
       │
       ▼
  触发高亮刷新（Pinia $subscribe）
```

### 5.4 阅读页渲染流程

```
用户打开经书
       │
       ▼
  SutraService.getChapter(sutraId, chapterIndex)
       │
       ▼
  获取章节内容（IndexedDB）
       │
       ▼
  高亮引擎处理
  - Highlighter.highlight(content, activeDictIds)
  - HTML 字符串渲染
       │
       ▼
  段落懒渲染
  - 视口内段落立即渲染
  - 视口外段落占位
  - IntersectionObserver 检测进入视口
       │
       ▼
  渲染到 DOM（innerHTML）
       │
       ▼
  绑定高亮点击事件（事件委托）
       │
       ▼
  恢复阅读进度（scrollTo）
```

---

## 6. 禅意设计系统

> **设计哲学**：界面退让，让经文内容成为主角。灵感来自 Ollama 的零阴影哲学、Apple 的阅读节奏、Notion 的粉彩分类系统、Linear 的暗色表面阶梯。详见 `docs/design/ZEN-DESIGN.md`（完整 DESIGN.md 规范）和 `docs/design/awesome-design-md-analysis.md`（调研来源）。

### 6.1 设计原则

| 原则 | 说明 | 来源 |
|------|------|------|
| **零阴影** | 深度通过背景色和边框实现，不使用阴影 | Ollama, Linear |
| **二元圆角** | 8px（容器）或 9999px（交互元素），无中间值 | Ollama |
| **单强调色** | 檀木色 (#8b7355) 作为唯一交互色 | Apple |
| **17px 正文** | Apple 风格阅读节奏，行高 1.65 | Apple |
| **语义化命名** | ink/hairline/surface 等语义化 token 名 | Notion, Linear |
| **四级表面阶梯** | 暗色模式通过表面阶梯区分层级 | Linear |

### 6.2 Token 系统（tokens.css）

```css
/* styles/tokens.css */

:root {
  /* ===== 色彩系统 — 日间主题 ===== */
  
  /* 品牌色 */
  --zen-accent: #8b7355;           /* 檀木色 — 唯一交互强调色 */
  --zen-accent-deep: #6b5540;      /* 檀木深 — 按下状态 */
  --zen-accent-light: #a89279;     /* 檀木浅 — 悬停状态 */
  --zen-on-accent: #ffffff;        /* 强调色上的文字 */

  /* 表面色 */
  --zen-canvas: #ffffff;           /* 画布白 — 页面主背景 */
  --zen-surface: #f8f7f4;          /* 宣纸色 — 区域背景，微暖米白 */
  --zen-surface-soft: #fafaf8;     /* 宣纸柔 — 柔和区分区域 */

  /* 文字色 */
  --zen-ink: #2c2c2c;              /* 墨 — 标题和正文主色，近黑非纯黑 */
  --zen-ink-muted: #6b6b6b;        /* 淡墨 — 次要文字、说明 */
  --zen-ink-subtle: #999999;       /* 浅墨 — 辅助文字、占位符 */

  /* 分隔线 */
  --zen-hairline: #e8e6e1;         /* 细线 — 卡片边框、分隔线 */
  --zen-hairline-strong: #d0cdc6;  /* 细线强 — 输入框聚焦边框 */

  /* 语义色（低饱和，色盲安全） */
  --zen-success: #4a7c59;          /* 佛教绿 */
  --zen-warning: #c49a6c;          /* 佛教金 */
  --zen-error: #a85656;            /* 佛教红 */

  /* 高亮色（词典分类粉彩系统 — 灵感来自 Notion） */
  --zen-highlight-mind: #e8f5e9;   /* 静心类 — 浅绿背景 */
  --zen-highlight-sutra: #fff3e0;  /* 经文类 — 浅橙背景 */
  --zen-highlight-commentary: #e3f2fd; /* 注释类 — 浅蓝背景 */
  --zen-highlight-mantra: #f3e5f5; /* 咒语类 — 浅紫背景 */
  --zen-highlight-text-mind: #2e7d32;
  --zen-highlight-text-sutra: #e65100;
  --zen-highlight-text-commentary: #1565c0;
  --zen-highlight-text-mantra: #7b1fa2;
}

/* ===== 暗色主题 — 四级表面阶梯 ===== */
[data-theme="dark"] {
  /* 表面阶梯 */
  --zen-canvas: #1a1a1a;           /* 画布暗 — 默认页面背景 */
  --zen-surface: #242424;          /* 表面 1 — 卡片、面板 */
  --zen-surface-elevated: #2e2e2e; /* 表面 2 — 悬浮卡片、选中态 */
  --zen-surface-nav: #383838;      /* 表面 3 — 导航、下拉菜单 */

  /* 文字 */
  --zen-ink: #e0e0e0;              /* 墨暗 — 暗色模式主文字 */
  --zen-ink-muted: #888888;        /* 淡墨暗 — 次要文字 */
  --zen-ink-subtle: #666666;       /* 浅墨暗 — 辅助文字 */

  /* 分隔 */
  --zen-hairline: #333333;         /* 细线暗 — 暗色模式边框 */
  --zen-hairline-strong: #444444;

  /* 强调色 */
  --zen-accent: #a89279;
  --zen-accent-deep: #8b7355;
  --zen-accent-light: #c4a88a;
}

/* ===== 护眼主题 ===== */
[data-theme="eye-care"] {
  --zen-canvas: #f5e6c8;           /* 暖黄纸色 */
  --zen-surface: #eddbb0;
  --zen-surface-soft: #f0e0b8;
  --zen-ink: #3c2e1e;              /* 深棕墨 */
  --zen-ink-muted: #5c4e3e;
  --zen-ink-subtle: #7c6e5e;
  --zen-hairline: #d4c4a8;
  --zen-hairline-strong: #c4b498;
  --zen-accent: #6b5540;
}

/* ===== 字体栈 ===== */
:root {
  --zen-font-serif: "Noto Serif SC", "Source Han Serif SC", "SimSun", "宋体", serif;
  --zen-font-sans: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
  --zen-font-mono: "Noto Sans Mono", "SF Mono", "Consolas", monospace;
  --zen-font-sanskrit: "Noto Sans Devanagari", "Sanskrit 2003", var(--zen-font-serif);
}
```

### 6.3 排版系统

> 基于 Apple 的 SF Pro 排版原则：负字间距 headline、17px 正文节奏、600/400 字重阶梯。

| Token | 大小 | 字重 | 行高 | 字间距 | 用途 |
|---|---|---|---|---|---|
| `--zen-display` | 40px | 600 | 1.15 | -1px | 页面大标题 |
| `--zen-h1` | 32px | 600 | 1.25 | -0.5px | 区块标题 |
| `--zen-h2` | 24px | 600 | 1.30 | 0 | 卡片标题 |
| `--zen-h3` | 20px | 600 | 1.35 | 0 | 小节标题 |
| `--zen-body-lg` | 18px | 400 | 1.65 | 0 | 引导段落 |
| `--zen-body` | 17px | 400 | 1.65 | 0 | 经文正文（Apple 风格） |
| `--zen-body-sm` | 14px | 400 | 1.50 | 0 | 辅助说明 |
| `--zen-caption` | 12px | 400 | 1.40 | 0 | 注释、元数据 |
| `--zen-button` | 14px | 500 | 1.30 | 0 | 按钮文字 |
| `--zen-tag` | 12px | 500 | 1.30 | 0 | 标签、徽章 |

**排版原则**：
1. **Display 使用负字间距** — 创造紧凑专业的标题感觉
2. **正文 17px，行高 1.65** — 比常规 1.50 更宽松，适合长文阅读
3. **标题 600，正文 400** — 避免使用 700（太重）和 500（暧昧）
4. **宋体标题 + 黑体正文** — 传统与现代结合

### 6.4 间距系统

> 基于 4px 基础单位，参考 Notion 和 Linear 的间距阶梯。

| Token | 值 | 用途 |
|---|---|---|
| `--zen-space-xxs` | 4px | 紧凑间距 |
| `--zen-space-xs` | 8px | 元素内间距 |
| `--zen-space-sm` | 12px | 小组件间距 |
| `--zen-space-md` | 16px | 标准内间距 |
| `--zen-space-lg` | 24px | 卡片内间距 |
| `--zen-space-xl` | 32px | 区块间距 |
| `--zen-space-2xl` | 48px | 大区块间距 |
| `--zen-space-section` | 64px | 页面区块间距 |

### 6.5 圆角系统

> Ollama 二元圆角系统：8px（容器）或 9999px（交互元素），无中间值。

| Token | 值 | 用途 |
|---|---|---|
| `--zen-radius-container` | 8px | 卡片、面板、输入框容器 |
| `--zen-radius-interactive` | 9999px | 按钮、标签、开关、搜索栏 |

### 6.6 深度与提升系统

> 零阴影哲学 — 深度通过背景色和边框实现。

| 层级 | 处理 | 用途 |
|---|---|---|
| 0 (平) | 无阴影，无边框 | 默认内容、文字、页面背景 |
| 1 (边框) | `1px solid var(--zen-hairline)` | 卡片、面板 |
| 2 (悬浮) | `1px solid var(--zen-hairline-strong)` + `var(--zen-surface)` 背景 | 悬停卡片、选中态 |
| 3 (模态) | 背景遮罩 + `var(--zen-surface-elevated)` | 弹窗、下拉菜单 |

**阴影哲学**：整个系统零阴影。深度通过以下方式传达：
1. 背景色变化（canvas → surface → surface-elevated）
2. 1px 头发线边框
3. 内容层级和排版权重

### 6.7 组件规范

#### 按钮

| 组件 | 背景 | 文字 | 边框 | 圆角 | 内边距 |
|---|---|---|---|---|---|
| `button-primary` | `var(--zen-accent)` | `var(--zen-on-accent)` | 无 | `--zen-radius-interactive` | 10px 20px |
| `button-secondary` | `transparent` | `var(--zen-accent)` | `1px solid var(--zen-accent)` | `--zen-radius-interactive` | 10px 20px |
| `button-ghost` | `transparent` | `var(--zen-ink-muted)` | 无 | `--zen-radius-interactive` | 8px 14px |

#### 卡片

| 组件 | 背景 | 边框 | 圆角 | 内边距 |
|---|---|---|---|---|
| `card-base` | `var(--zen-canvas)` | `1px solid var(--zen-hairline)` | `--zen-radius-container` | 20px |
| `card-sutra` | `var(--zen-canvas)` | `1px solid var(--zen-hairline)` | `--zen-radius-container` | 24px |

#### 输入框

| 状态 | 背景 | 文字 | 边框 | 圆角 | 高度 |
|---|---|---|---|---|---|
| 默认 | `var(--zen-canvas)` | `var(--zen-ink)` | `1px solid var(--zen-hairline)` | `--zen-radius-interactive` | 44px |
| 聚焦 | `var(--zen-canvas)` | `var(--zen-ink)` | `2px solid var(--zen-accent)` | `--zen-radius-interactive` | 44px |

#### 标签

| 组件 | 背景 | 文字 | 圆角 | 内边距 |
|---|---|---|---|---|
| `tag-pill` | `var(--zen-surface)` | `var(--zen-ink-muted)` | `--zen-radius-interactive` | 4px 12px |
| `tag-category-mind` | `var(--zen-highlight-mind)` | `var(--zen-highlight-text-mind)` | `--zen-radius-interactive` | 4px 12px |
| `tag-category-sutra` | `var(--zen-highlight-sutra)` | `var(--zen-highlight-text-sutra)` | `--zen-radius-interactive` | 4px 12px |
| `tag-category-commentary` | `var(--zen-highlight-commentary)` | `var(--zen-highlight-text-commentary)` | `--zen-radius-interactive` | 4px 12px |
| `tag-category-mantra` | `var(--zen-highlight-mantra)` | `var(--zen-highlight-text-mantra)` | `--zen-radius-interactive` | 4px 12px |

### 6.8 设计护栏 (Do's & Don'ts)

#### Do
- 使用 `var(--zen-accent)` 檀木色作为唯一的交互强调色
- 所有交互元素使用 `--zen-radius-interactive` (9999px) 药丸形圆角
- 容器使用 `--zen-radius-container` (8px) 圆角
- 正文使用 17px，行高 1.65
- 深度通过背景色变化实现，不使用阴影
- 保持内容密度低 — 每个区域传达一个清晰概念
- 暗色模式使用四级表面阶梯

#### Don't
- 不要引入第二个强调色
- 不要对卡片或按钮使用阴影
- 不要使用 700+ 字重
- 不要混用圆角 — 坚持二元系统（8px 或 9999px）
- 不要使用装饰性渐变
- 不要让界面元素与经文内容竞争注意力

### 6.9 响应式策略

| 断点 | 宽度 | 关键变化 |
|---|---|---|
| 手机 | < 480px | 单列，标题 24px |
| 大手机 | 480-767px | 单列，标题 28px |
| 平板 | 768-1023px | 2 列网格 |
| 桌面 | 1024-1279px | 3-4 列网格 |
| 宽桌面 | ≥ 1280px | 最大宽度锁定 1280px |

**触摸目标**：最小 44x44px，按钮高度 40-44px，输入框高度 44px。

---

## 7. 性能指标

### 7.1 核心性能目标

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| **首屏加载（FCP）** | < 1s | Lighthouse |
| **最大内容绘制（LCP）** | < 2.5s | Lighthouse |
| **可交互时间（TTI）** | < 3.5s | Lighthouse |
| **首屏 Bundle** | JS < 150KB, CSS < 30KB (gzip) | rollup-plugin-visualizer |
| **Trie 构建（10 万词条）** | < 100ms（Worker） | performance.now() |
| **高亮响应（1000 字）** | < 10ms | performance.now() |
| **高亮响应（5000 字）** | < 30ms | performance.now() |
| **释义加载** | < 200ms | performance.now() |
| **DOMPurify 净化（短）** | < 2ms | performance.now() |
| **DOMPurify 净化（长）** | < 15ms | performance.now() |
| **MDX 预解析（<5MB）** | < 1s | performance.now() |
| **Store 内存上限** | < 500KB（序列化） | performance.memory |

### 7.2 分场景内存估算

| 场景 | 常驻内存 | 峰值内存 |
|------|----------|----------|
| 仅内置词典（50 条） | < 1MB | < 2MB |
| 内置 + 1 个用户词典（1 万条） | ~3MB | ~15MB |
| 内置 + 5 个用户词典（各 2 万条） | ~8MB | ~50MB |
| 极端场景（10 万条） | ~25MB | ~200MB |

---

## 8. 安全策略

### 8.1 四层防护

| 层级 | 措施 | 说明 |
|------|------|------|
| **1. 文件验证** | 类型 + 大小 | .mdx/.json/.csv，大小 ≤ 10MB |
| **2. MDX 扫描** | 恶意内容检测 | script 标签、事件处理器、javascript: 协议 |
| **3. DOMPurify** | HTML 净化 | 所有 v-html 前必须净化 |
| **4. CSP** | Content-Security-Policy | `script-src 'self'`、`object-src 'none'` |

### 8.2 CSP 配置（vercel.json）

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self'; object-src 'none'; frame-src 'none'; base-uri 'self'; form-action 'self'"
        },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "X-XSS-Protection", "value": "1; mode=block" }
      ]
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

---

## 9. 部署与 CI/CD

### 9.1 Vercel 部署配置

| 配置项 | 值 |
|--------|-----|
| **框架预设** | Vite |
| **构建命令** | `npm run build` |
| **输出目录** | `dist` |
| **Node.js 版本** | 18.x |
| **环境变量** | `VERCEL_ENV`（production/preview/development） |

### 9.2 GitHub Actions CI

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
      - run: npm run build
```

---

## 10. 风险清单

### 10.1 技术风险

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| iOS Safari 50MB 硬限 | 高 | 高 | 上传前预检，超限提示 |
| Worker 内存超限静默终止 | 中 | 高 | 分片构建（5000 词条/批） |
| MDX 格式不兼容（v3.0/加密） | 中 | 中 | 仅支持 v1.2-v2.0，加密提示 |
| mdict-js 小众库维护 | 中 | 中 | 锁定 v10.0.1 MIT 分支 |

### 10.2 兼容性风险

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| iOS 15.0-15.3 ESM Worker 不支持 | 低 | 中 | 标记"部分兼容" |
| Chrome 无痕模式临时存储 | 中 | 中 | 提示用户数据不持久 |
| 多标签页并发读写冲突 | 低 | 中 | `versionchange` 事件通知 |

---

## 附录

### A. 用户决策确认清单

| 决策项 | 结论 | 确认日期 |
|--------|------|----------|
| D-4 书架页面范围 | v2.0 仅列表，v2.1 加功能 | 2026-05-02 |
| D-5 功德统计页面 | v2.0 暂不做 | 2026-05-02 |
| D-6 iOS Worker 降级 | 静默降级，用户无感知 | 2026-05-02 |

### B. 52 个调研文档索引

| 类别 | 文档 | 状态 |
|------|------|------|
| 技术调研 | T-01 ~ T-15 | ✅ 已完成 |
| 功能分析 | T-16 ~ T-28 | ✅ 已完成 |
| 决策验证 | T-29 ~ T-36 | ✅ 已完成 |
| 深度分析 | T-37 ~ T-50 | ✅ 已完成 |
| 综合输出 | T-51 ~ T-52 | ✅ 已完成 |
